from prophet import Prophet
import itertools

# Grille de paramètres
param_grid = {  
    'changepoint_prior_scale': [0.001, 0.01, 0.05, 0.1, 0.5], # Rigidité de la tendance
    'seasonality_prior_scale': [0.01, 0.1, 1.0, 10.0],        # Rigidité de la saisonnalité
}

# Générer les combinaisons
all_params = [dict(zip(param_grid.keys(), v)) for v in itertools.product(*param_grid.values())]
best_rmse = float('inf')
best_params = {}

print("Optimisation Prophet en cours...")

for params in all_params:
    # On configure le modèle avec les params actuels
    m = Prophet(yearly_seasonality=True, weekly_seasonality=False, daily_seasonality=False, **params)
    m.fit(df_prophet) # df_prophet doit contenir le TRAIN uniquement !
    
    # Prévision sur le test
    future = m.make_future_dataframe(periods=n_test, freq='Q')
    forecast = m.predict(future)
    pred_values = forecast['yhat'].iloc[-n_test:].values
    
    rmse = np.sqrt(mean_squared_error(test, pred_values))
    
    if rmse < best_rmse:
        best_rmse = rmse
        best_params = params

print(f"Meilleurs params Prophet : {best_params}")
print(f"RMSE Prophet Optimisé : {best_rmse:.4f}")

# 1. RENDRE LA SÉRIE STATIONNAIRE (Indispensable pour XGBoost sur données avec tendance)
df_ml = pd.DataFrame(ts_ecart.copy())
df_ml.columns = ['y']
# On prédit la variation (diff), pas la valeur absolue
df_ml['y_diff'] = df_ml['y'].diff() 

# 2. Feature Engineering sur la Différence
df_ml['lag_1'] = df_ml['y_diff'].shift(1)
df_ml['lag_2'] = df_ml['y_diff'].shift(2)
df_ml['lag_4'] = df_ml['y_diff'].shift(4) # Saisonnalité

df_ml = df_ml.dropna()

# 3. Split Train/Test
train_size = len(df_ml) - n_test
X = df_ml[['lag_1', 'lag_2', 'lag_4']]
y = df_ml['y_diff']

X_train, y_train = X.iloc[:train_size], y.iloc[:train_size]
X_test, y_test = X.iloc[train_size:], y.iloc[train_size:]

# 4. Entraînement (RandomizedSearch est très bien, gardez-le)
rs.fit(X_train, y_train)
best_xgb = rs.best_estimator_

# 5. PRÉVISION RÉCURSIVE (Sans triche)
# On récupère la dernière fenêtre connue du Train
last_window = X_train.iloc[-1].values 
# last_window = [lag_1, lag_2, lag_4]

predictions_diff = []
current_lags = last_window.copy()

for i in range(n_test):
    # Prédire la variation pour le pas suivant
    # Reshape car predict attend un tableau 2D
    pred_diff = best_xgb.predict(current_lags.reshape(1, -1))[0]
    predictions_diff.append(pred_diff)
    
    # Mettre à jour les lags pour le tour suivant (Shift manuel)
    # lag_1 devient la prédiction qu'on vient de faire !
    # lag_2 devient l'ancien lag_1, etc.
    new_lags = np.array([pred_diff, current_lags[0], current_lags[2]]) 
    # Note : lag_4 est plus complexe à mettre à jour en récursif strict, 
    # ici on simplifie ou on récupère l'historique plus loin.
    current_lags = new_lags

# 6. RECONSTRUCTION (Diff -> Niveau)
# On repart de la dernière valeur réelle connue du Train
last_real_value = train.iloc[-1]
pred_xgb_levels = []
current_val = last_real_value

for diff in predictions_diff:
    current_val = current_val + diff
    pred_xgb_levels.append(current_val)

rmse_xgb_recursive = np.sqrt(mean_squared_error(test, pred_xgb_levels))

print(f"RMSE XGBoost (Récursif + Diff) : {rmse_xgb_recursive:.4f}")
