import polars as pl
import numpy as np
from scipy.stats import wasserstein_distance

# ---- CONFIG ----
CLASS_COL = "CHR_model"
LGD_COL   = "lgd_obligor"
df_base   = base
# ----------------

def cohen_d(a1, a2):
    n1, n2 = len(a1), len(a2)
    s1, s2 = a1.std(ddof=1), a2.std(ddof=1)
    sp = np.sqrt(((n1 - 1) * s1**2 + (n2 - 1) * s2**2) / (n1 + n2 - 2))
    return (a2.mean() - a1.mean()) / sp if sp > 0 else 0.0

def overlap(a1, a2, n_bins=50):
    lo, hi = min(a1.min(), a2.min()), max(a1.max(), a2.max())
    if hi <= lo:
        return 1.0
    bins = np.linspace(lo, hi, n_bins)
    p, _ = np.histogram(a1, bins=bins); q, _ = np.histogram(a2, bins=bins)
    p, q = p / p.sum(), q / q.sum()
    return float(np.minimum(p, q).sum())

def diagnostics_tailles_effet_modele(
    df,
    class_col: str = CLASS_COL,
    lgd_col: str = LGD_COL,
) -> pl.DataFrame:
    """
    Paires de CHR_model ADJACENTES en ordre LRA GLOBAL (pas de cloisonnement).
    Tailles d'effet INDÉPENDANTES de n : Cohen's d, OVL, Wasserstein-1.
    Ce sont elles qui disent si un rassemblement supplémentaire est justifiable.
    """
    lf = df.lazy() if isinstance(df, pl.DataFrame) else df

    ordre = (
        lf.group_by([class_col, "year"]).agg(pl.col(lgd_col).mean().alias("m"))
          .group_by([class_col]).agg(pl.col("m").mean().alias("mean_LGD_global"))
          .sort("mean_LGD_global")
          .collect()
    )

    obligors = {}
    base_col = lf.select([class_col, lgd_col]).collect()
    for cls in base_col.select(class_col).unique().to_series():
        obligors[cls] = base_col.filter(pl.col(class_col) == cls).get_column(lgd_col).to_numpy()

    rows = []
    classes = ordre.get_column(class_col).to_list()
    for i in range(len(classes) - 1):
        c1, c2 = classes[i], classes[i + 1]
        a1, a2 = obligors[c1], obligors[c2]
        rows.append({
            "class_1": c1, "class_2": c2,
            "n_1": len(a1), "n_2": len(a2),
            "gap": float(abs(a2.mean() - a1.mean())),
            "cohen_d": float(abs(cohen_d(a1, a2))),
            "ovl": overlap(a1, a2),
            "wasserstein": float(wasserstein_distance(a1, a2)),
        })
    return pl.DataFrame(rows)


# ---- Exécution ----
eff = diagnostics_tailles_effet_modele(df_base)

print("=" * 90)
print("DIAGNOSTIC niveau MODÈLE — tailles d'effet (indépendantes de n) sur CHR_model adjacentes")
print("=" * 90)

print(f"\nDistribution sur les {eff.height} paires adjacentes :\n")
for col in ["gap", "cohen_d", "ovl", "wasserstein"]:
    s = eff.get_column(col)
    print(f"{col:12s}  min={s.min():.3f}  q25={s.quantile(.25):.3f}  med={s.median():.3f}  "
          f"q75={s.quantile(.75):.3f}  max={s.max():.3f}")

print("\nLes 15 paires les plus PROCHES (Wasserstein croissant) — candidates au rassemblement :")
with pl.Config(tbl_rows=15, tbl_cols=-1, tbl_width_chars=220, float_precision=4):
    print(eff.sort("wasserstein").head(15))

# Combien de paires passeraient des seuils d'effet "fusionnables" ?
for d_max, ovl_min, w_max in [(0.5, 0.30, 2.0), (0.8, 0.20, 3.0), (1.0, 0.15, 4.0)]:
    n_ok = eff.filter(
        (pl.col("cohen_d") < d_max) & (pl.col("ovl") > ovl_min) & (pl.col("wasserstein") < w_max)
    ).height
    print(f"\nSeuils  d<{d_max}, OVL>{ovl_min}, W<{w_max}  ->  {n_ok} paires fusionnables / {eff.height}")
