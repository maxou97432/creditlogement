n = srs_X_capret.filter(
    (pl.col('duration').clip(1.0, 5.0) - pl.col('NB_DURATION_IRBA')).abs() < 0.001
).height
total = srs_X_capret.height
print(f"clip(duration_capret, 1,5) vs NB_DURATION_IRBA : {n}/{total} ({n/total:.1%})")
