print(">>> compute_maturite_irba INPUT schema:", lf.collect_schema().names())
    print(">>> DT_ARRT in input?", "DT_ARRT" in lf.collect_schema().names())
    print(">>> D_ARRETE in input?", "D_ARRETE" in lf.collect_schema().names())
    
    lf = lf.with_columns(
        date_to_sas('DT_ARRT').alias('DT_ARRT_sas')
    )

def build_pipeline(cap_pret_lf, *, ...):
    lf = _normalize(cap_pret_lf, ...)
    print(">>> build_pipeline après _normalize:", "DT_ARRT" in lf.collect_schema().names())
    
    if DURATION_MODE == "NEW":
        lf = compute_maturite_irba(lf)
    ...
