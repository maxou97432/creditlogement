# Filtre ANCIENNETE=ENSEMBLE pour être comparable avec INSEE
        # (sans ce filtre, 3 lignes CL par (zone × type × période) → doublons au merge)
        if "ANCIENNETE" in df_final.columns:
            df_final = df_final[df_final["ANCIENNETE"] == "ENSEMBLE"].copy()
            df_final = df_final.drop(columns=["ANCIENNETE"])
