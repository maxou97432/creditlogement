d11 = final_survival_rate.filter(
    (pl.col(CFG["jenks_submodel"]) == "D_11") & (pl.col("generation").is_in([2013, 2014, 2015]))
)
print(d11.select(["CRR__final", "CAD_act__final"]).describe())

# Et surtout, regarder le dev_factor du CAD pour cette classe précise
# (il faut aller chercher results["D_11"]["dev_factor_mCAD_act_"] dans ton dict de résultats)
print(results["D_11"]["dev_factor_mCAD_act_"])
print(results["D_11"]["n_open_cases"])  # pour voir si l'effectif est faible à certains horizons
