# Sur quelles lignes les deux bases s'accordent-elles ?
accord = srs_X_capret.filter(pl.col('Ecart_brut').abs() < 0.001)
print(f"Lignes en accord : {accord.height}")
print(f"Leur Type_Credit : {accord.group_by('Type_Credit').len().sort('len', descending=True)}")

# Et les grosses divergences ?
gros_ecarts = srs_X_capret.filter(pl.col('Ecart_brut').abs() > 1.0)
print(f"\nEcarts > 1 an : {gros_ecarts.height}")
print(gros_ecarts.select(['NB_DURATION_IRBA', 'duration', 'Ecart_brut', 'Type_Credit']).head(10))
