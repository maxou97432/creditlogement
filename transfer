# --- Cellule taguée "parameters" ---
chemin_entree = None
chemin_sortie = None
nom_notebook  = "2 - Rassemblement niveau Modèle.ipynb"   # repli standalone uniquement

# --- Cellule suivante ---
from pathlib import Path
from utils.config import load_config, get_notebook_io

CFG = load_config("config.yaml")          # variables métier : toujours depuis le YAML

if chemin_entree is None and chemin_sortie is None:
    CHEMIN_ENTREE, CHEMIN_SORTIE = get_notebook_io(CFG, nom_notebook)
    print(f"[standalone] {nom_notebook}")
else:
    CHEMIN_ENTREE = Path(chemin_entree) if chemin_entree else None
    CHEMIN_SORTIE = Path(chemin_sortie) if chemin_sortie else None
    print(f"[orchestré] {nom_notebook}")

LGD_COL, EAD_COL = CFG["lgd_calc"], CFG["ead"]
FACILITE, OBLIGOR = CFG["facility"], CFG["obligor"]
DATDEF, SEG = CFG["date_defaut"], CFG["segment_col"]
