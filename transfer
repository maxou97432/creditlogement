lf_LGD2 = (
    lf_LGD
    .join(
        df_obligor.select([config['Obligor'], config['Date_defaut'],
                           "Jenks_Submodel", "lgd_obligor"]),
        on=[config['Obligor'], config['Date_defaut'], "Jenks_Submodel"],
        how="left",
    )
    .join(
        df_class.select(["Jenks_Submodel", "lgd_class"]),
        on="Jenks_Submodel",
        how="left",
    )
)
