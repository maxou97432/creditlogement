rd = rd[rd[COL_NOM].isin(colonnes_presentes)]

# Exclusion des identifiants (ID_RC, Obligor_ID, etc.) : la colonne Risk_drivers
# de l'Excel les tague explicitement, pour ne pas les traiter comme de vrais
# risk drivers quali/quanti
COL_RISK_DRIVERS = "Risk_drivers"  # à ajuster si le nom exact diffère
if COL_RISK_DRIVERS in rd.columns:
    avant = len(rd)
    rd = rd[rd[COL_RISK_DRIVERS] == "RISK_DRIVER"]
    print(f"Filtre Risk_drivers : {avant} -> {len(rd)} lignes conservées")
else:
    print(f"⚠️ Colonne '{COL_RISK_DRIVERS}' absente de l'Excel — "
          f"filtre ID non appliqué, vérifier manuellement drivers_quali/drivers_quanti")

drivers_quali  = rd.loc[rd[COL_TYPE].str.upper().str.startswith("C"), COL_NOM].tolist()
drivers_quanti = rd.loc[rd[COL_TYPE].str.upper().str.startswith("Q"), COL_NOM].tolist()
