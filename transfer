n_ok = base.filter(
    pl.col("NB_DURATION_IRBA").round(3) == pl.col("NB_MATURITE_IRBA").clip(1.0, 5.0).round(3)
).height

print(f"Confirmé par clip : {n_ok} / {base.height} ({100*n_ok/base.height:.1f}%)")
