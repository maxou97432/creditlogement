test = (
    lf_binned.select(["annee", "PRD_VB001_CART", TARGET])
    .collect()
    .group_by(["annee", "PRD_VB001_CART"])
    .agg([
        pl.col(TARGET).mean().alias("target_moyenne"),
        pl.len().alias("effectif"),
    ])
)
print(test)
