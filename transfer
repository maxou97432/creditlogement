lf_binned, seuils_cart, drivers_quanti_bines = appliquer_decoupage_cart(
    lf=lf,
    variables_quanti=drivers_quanti,
    target=TARGET,
    distribution=DISTRIBUTION,
    max_leaf_nodes=MAX_LEAF_NODES,
    min_samples_leaf=MIN_SAMPLES_LEAF,
    dossier_sortie=OUTPUT_DIR,
)

df_seuils = pd.DataFrame([
    {"variable": k, "nb_classes": v["nb_classes"], "colonne_binee": v["colonne_binee"]}
    for k, v in seuils_cart.items()
])
df_seuils.head(20)
