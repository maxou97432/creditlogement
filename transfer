residus = pop[(pop["NB_DURATION_SIMULE_V3"] - pop["NB_DURATION_IRBA"]).abs() >= 1e-6]

# Écart RWA estimé : comparer RWA actuel vs RWA qu'on aurait avec notre M reconstituée
# (nécessite un recalcul complet de K, mais approximation linéaire avec dérivée en M)
print(f"Nombre de résidus : {len(residus):,}")
print(f"RWA cumulé des résidus : {residus['RWA_IRBA'].sum():,.0f}")
print(f"RWA total Corporate   : {pop['RWA_IRBA'].sum():,.0f}")
print(f"Part RWA résiduelle   : {100*residus['RWA_IRBA'].sum()/pop['RWA_IRBA'].sum():.3f}%")

NB_DURATION_IRBA = 
    IF   CD_OBJ_FIN = 'FA2'                            → NB_MATURITE_IRBA
    ELIF CD_OBJ_FIN = 'GA5' 
         AND TX_CCF_IRBA_PNU ≤ 0.27 
         AND DT_DEBUT_AUTO > 23276 (≈ 2023-09-26)      → NB_MATURITE_IRBA
    ELSE                                                → clip(NB_MATURITE_IRBA, 1.0, 5.0)
