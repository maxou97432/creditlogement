# Colonnes en conflit de type à trancher (force vs drop)
# ---------------------------------------------------------------
cols_a_verifier = ["PRD_VHR32", "PRD_VS37", "PRD_VS35", "PRD_TYPECONTRAT"]

resultats_usage = check_variables_utilisees(cols_a_verifier, segments)

print(" Analyse d'usage des colonnes en conflit de type :")
for col, usage in resultats_usage.items():
    detail = ", ".join(f"{t}={'✅' if u else '❌'}" for t, u in usage.items())
    verdict = "🔧 force" if any(usage.values()) else "🗑️ drop"
    print(f"  - {col:20s} [{detail}] -> {verdict}")

column_actions_auto = deriver_column_actions(resultats_usage)

column_actions = {
    # "RET_CORP": "drop",      # on la supprime partout
    **column_actions_auto,
}

# Définir le type cible pour les colonnes en mode "force"
# ---------------------------------------------------------------
force_map = {"Jenks_class": pl.Int64}  # à compléter si une colonne ressort en "force"

# Appliquer le traitement
# ---------------------------------------------------------------
lazy_cleaned = handle_type_conflict_per_column(
    lazy_by_name=lazy_by_name,
    type_mismatch_by_type=type_mismatch_by_type,
    default_action="force",
    column_actions=column_actions,
    force_map=force_map,
)
