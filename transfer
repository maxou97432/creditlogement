cmp = FusionComparator(
    df,
    class_col="CHR_apres_fusion",
    lgd_col="lgd_obligor",
    obligor_col="ID_CLIENT",
    pairs="adjacent",
    tests=[OVLPermutationTest()],   # uniquement celui-là
).run()

# vue détaillée longue : une ligne par (paire, test)
cmp.detail()

# vue large : une ligne par paire, colonnes = tests, avec OVL et verdicts
cmp.matrix()

# heatmap visuelle
cmp.plot_heatmap()
