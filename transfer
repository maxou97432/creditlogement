import polars as pl
w = df.select(pl.col("ead_fin")).collect().to_series()
print("min:", w.min(), "max:", w.max(), "% <= 0:", (w <= 0).mean(), "% == 0:", (w == 0).mean())
filtered_lf_lgd = filtered_lf_lgd.with_columns(
    pl.col("ead_fin").abs().alias("ead_poids")
)
w = filtered_lf_lgd.select(pl.col("ead_poids")).collect().to_series()
print("total:", w.sum(), "min:", w.min(), "% > 0:", (w > 0).mean())  # total > 0, % > 0 ≈ 100%
