if isinstance(CHEMIN_ENTREE, dict):
    chemin_curerate = Path(CHEMIN_ENTREE["curerate"])
    chemin_noncure  = Path(CHEMIN_ENTREE["noncure"])
else:
    raise TypeError(
        f"CHEMIN_ENTREE attendu sous forme de dict (curerate/noncure) pour ce notebook, "
        f"reçu : {type(CHEMIN_ENTREE)}"
    )

lf_CureRate = pl.scan_parquet(chemin_curerate)
lf_NonCure  = pl.scan_parquet(chemin_noncure)
