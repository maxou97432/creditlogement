import yaml
from pathlib import Path
import numpy as np
import polars as pl
import pandas as pd

from utils.stat_desc import (
    appliquer_decoupage_cart, stats_par_modalite,
    export_brut_comptages, exporter_stat_desc_excel,
)
from utils.output import cramers_v_matrix

with open(config_path, encoding="utf-8") as f:
    config = yaml.safe_load(f)

if not config.get("run_stat_desc", False):
    print("run_stat_desc = False -> notebook ignoré.")
    raise SystemExit

TARGET       = config["target"]
DISTRIBUTION = config.get("distribution", "continuous")
ID_COL       = config.get("Granularity", "FACILITY")
DATE_PHOTO   = config["Date_photo"]
OUTPUT_DIR   = Path(config["Output"])
RD_PATH      = Path(config["RD"])

# Paramètres propres au notebook
MAX_LEAF_NODES   = config.get("cart_max_leaf_nodes", 5)
MIN_SAMPLES_LEAF = config.get("cart_min_samples_leaf", 0.05)
NB_DRIVERS_MAX   = config.get("nb_drivers_max_correlation", None)  # None = tous

print(f"Target : {TARGET} ({DISTRIBUTION}) | ID : {ID_COL} | Sortie : {OUTPUT_DIR}")
