import polars as pl

mask_bad = final_survival_rate["LRAfin_REBOOTp"] > 1

print(f"Nb de lignes LGD > 100% : {mask_bad.sum()}")

print(
    final_survival_rate.filter(pl.col("LRAfin_REBOOTp") > 1)
    .select(["CRR__final", "CAD_act__final", "CRRadj", "LRAfin_REBOOTp", "CLOS"])
    .describe()
)

print(
    final_survival_rate.filter(pl.col("LRAfin_REBOOTp") > 1)
    .select(["CRR__final", "CAD_act__final"])
    .head(10)
)
