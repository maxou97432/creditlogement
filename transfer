# ============================================================================
# APPROCHE 2 : ÉCART DE MOYENNES + COHEN'S D + OVERLAP (CORRIGÉ)
# ============================================================================
def meilleur_candidat_fusion_v2_robust(
    df: pl.LazyFrame,
    nb_croisements_max: int = 0,
    ans_strict: int = 0,
    lgd_col: str = "lgd_obligor",
    jenks_col: str = "submodel_class",
    log: bool = False,
) -> Tuple[Optional[List[str]], Optional[pl.DataFrame]]:
    """
    Critère : fusion si:
      1. |mean_A - mean_B| < DELTA_LGD (matérialité)
      2. |Cohen's d| < D_MAX (faible effet)
      3. OVL > OVL_MIN (fort recouvrement)
    """
    ordre_global = (
        df.group_by([jenks_col, "year"])
        .agg(pl.col(lgd_col).mean().alias("mean_LGD_year"))
        .group_by([jenks_col])
        .agg(
            pl.col("mean_LGD_year").mean().alias("mean_LGD_global"),
            pl.col("mean_LGD_year").std(ddof=1).alias("std_LGD_global"),
            pl.len().alias("n_years"),
        )
        .with_columns(pl.col(jenks_col).str.split("_").list.first().alias("seg"))
        .sort(["seg", "mean_LGD_global"])
    )

    # Récupérer obligors par classe (FIX : itération correcte)
    obligors_per_class = {}
    df_collected = df.select([jenks_col, lgd_col]).collect()
    for cls_name in df_collected.select(jenks_col).unique().to_series():
        obligors_per_class[cls_name] = (
            df_collected.filter(pl.col(jenks_col) == cls_name)
            .select(lgd_col)
            .to_numpy()
            .flatten()
        )

    paires_list = []
    ordre_collected = ordre_global.collect()
    
    for i in range(len(ordre_collected) - 1):
        row = ordre_collected[i]
        seg = row[jenks_col].str.split("_").to_list()[0]
        cls_1 = row[jenks_col]
        mean_1 = row["mean_LGD_global"]
        std_1 = row["std_LGD_global"] or 0.001

        # Trouver voisin intra-segment suivant
        found_next = False
        for j in range(i + 1, len(ordre_collected)):
            next_row = ordre_collected[j]
            next_seg = next_row[jenks_col].str.split("_").to_list()[0]
            if next_seg == seg:
                cls_2 = next_row[jenks_col]
                mean_2 = next_row["mean_LGD_global"]
                std_2 = next_row["std_LGD_global"] or 0.001
                found_next = True
                break
        
        if not found_next:
            continue

        ecart_mean = abs(mean_2 - mean_1)
        if ecart_mean >= DELTA_LGD:
            continue

        n1 = len(obligors_per_class[cls_1])
        n2 = len(obligors_per_class[cls_2])
        sp = np.sqrt(((n1 - 1) * std_1**2 + (n2 - 1) * std_2**2) / (n1 + n2 - 2))
        cohens_d = ecart_mean / sp if sp > 0 else 0
        if abs(cohens_d) >= D_MAX:
            continue

        arr1 = obligors_per_class[cls_1]
        arr2 = obligors_per_class[cls_2]
        lo, hi = min(arr1.min(), arr2.min()), max(arr1.max(), arr2.max())
        n_bins = max(1, int(np.ceil((hi - lo) / 0.25)))
        p, _ = np.histogram(arr1, bins=np.linspace(lo, hi, n_bins + 1))
        q, _ = np.histogram(arr2, bins=np.linspace(lo, hi, n_bins + 1))
        p, q = p / p.sum(), q / q.sum()
        ovl = np.minimum(p, q).sum()
        if ovl < OVL_MIN:
            continue

        paires_list.append({
            jenks_col: cls_1,
            "next_class": cls_2,
            "ecart_lgd": ecart_mean,
            "cohens_d": cohens_d,
            "ovl": ovl,
            "severite_totale": -ecart_mean,
        })

    paires = pl.DataFrame(paires_list) if paires_list else pl.DataFrame()

    if log:
        print("=== V2: Moyen + Cohen's d + OVL ===")
        print("Aucune paire." if paires.height == 0 else paires)

    if paires.height == 0:
        return None, None

    paires = paires.sort("severite_totale", descending=True).collect() if isinstance(paires, pl.LazyFrame) else paires.sort("severite_totale", descending=True)
    meilleur = paires.head(1)
    return [meilleur[jenks_col][0], meilleur["next_class"][0]], paires


# ============================================================================
# APPROCHE 3 : ÉCART DE MOYENNES + WASSERSTEIN (CORRIGÉ)
# ============================================================================
def meilleur_candidat_fusion_v3_wasserstein(
    df: pl.LazyFrame,
    nb_croisements_max: int = 0,
    ans_strict: int = 0,
    lgd_col: str = "lgd_obligor",
    jenks_col: str = "submodel_class",
    log: bool = False,
) -> Tuple[Optional[List[str]], Optional[pl.DataFrame]]:
    """
    Critère : fusion si:
      1. |mean_A - mean_B| < DELTA_LGD (matérialité)
      2. Wasserstein_1(A, B) < W_MAX (distributions proches, en points LGD)
    """
    ordre_global = (
        df.group_by([jenks_col, "year"])
        .agg(pl.col(lgd_col).mean().alias("mean_LGD_year"))
        .group_by([jenks_col])
        .agg(pl.col("mean_LGD_year").mean().alias("mean_LGD_global"))
        .with_columns(pl.col(jenks_col).str.split("_").list.first().alias("seg"))
        .sort(["seg", "mean_LGD_global"])
    )

    # Récupérer obligors (FIX : itération correcte)
    obligors_per_class = {}
    df_collected = df.select([jenks_col, lgd_col]).collect()
    for cls_name in df_collected.select(jenks_col).unique().to_series():
        obligors_per_class[cls_name] = (
            df_collected.filter(pl.col(jenks_col) == cls_name)
            .select(lgd_col)
            .to_numpy()
            .flatten()
        )

    paires_list = []
    ordre_collected = ordre_global.collect()
    
    for i in range(len(ordre_collected) - 1):
        row = ordre_collected[i]
        seg = row[jenks_col].str.split("_").to_list()[0]
        cls_1 = row[jenks_col]
        mean_1 = row["mean_LGD_global"]

        found_next = False
        for j in range(i + 1, len(ordre_collected)):
            next_row = ordre_collected[j]
            next_seg = next_row[jenks_col].str.split("_").to_list()[0]
            if next_seg == seg:
                cls_2 = next_row[jenks_col]
                mean_2 = next_row["mean_LGD_global"]
                found_next = True
                break
        
        if not found_next:
            continue

        ecart_mean = abs(mean_2 - mean_1)
        if ecart_mean >= DELTA_LGD:
            continue

        arr1 = obligors_per_class[cls_1]
        arr2 = obligors_per_class[cls_2]
        w = wasserstein_distance(arr1, arr2)
        if w >= W_MAX:
            continue

        paires_list.append({
            jenks_col: cls_1,
            "next_class": cls_2,
            "ecart_lgd": ecart_mean,
            "wasserstein": w,
            "severite_totale": -ecart_mean,
        })

    paires = pl.DataFrame(paires_list) if paires_list else pl.DataFrame()

    if log:
        print("=== V3: Moyen + Wasserstein ===")
        print("Aucune paire." if paires.height == 0 else paires)

    if paires.height == 0:
        return None, None

    paires = paires.sort("severite_totale", descending=True).collect() if isinstance(paires, pl.LazyFrame) else paires.sort("severite_totale", descending=True)
    meilleur = paires.head(1)
    return [meilleur[jenks_col][0], meilleur["next_class"][0]], paires
