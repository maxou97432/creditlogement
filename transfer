# Combien de lignes ont I_RIB_DAR null après le 1er join (srs → srs_unpooled) ?
print("I_RIB_DAR null après 1er join :", srs_complet['I_RIB_DAR'].null_count())
# → Si c'est proche de 295 000, le problème est là !

# Vérifier ce que contient srs_unpooled sur la clé ID_FEM
cles_srs         = set(srs['ID_FEM'].drop_nulls().unique().to_list())
cles_bridge      = set(srs_unpooled['ID_FEM'].drop_nulls().unique().to_list())
print(f"Clés ID_FEM communes srs ↔ srs_unpooled : {len(cles_srs & cles_bridge)}")
print(f"Clés ID_FEM dans srs ABSENTES de srs_unpooled : {len(cles_srs - cles_bridge)}")
