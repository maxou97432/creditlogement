# sur UNE catégorie, compare pandas (référence) et polars
cat = 1.0
ref  = extrapolate_all_categories(...)        # version pandas
new  = extrapolate_all_categories_lazy(...)    # version polars

r = ref[1].query("categories == @cat").groupby("generation")["CRR__final"].mean()
n = (new[1].filter(pl.col("categories")==cat)
        .group_by("generation").agg(pl.col("CRR__final").mean())
        .collect().to_pandas().set_index("generation")["CRR__final"])
print(pd.concat([r, n], axis=1, keys=["pandas","polars"]))
