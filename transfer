# 1. Combien de NB_MATURITE_IRBA sont null dans la base complète ?
print("=== NULL CHECK ===")
print(f"NB_MATURITE_IRBA null : {capret['NB_MATURITE_IRBA'].null_count()}/{capret.height}")

# 2. Parmi les mismatches, est-ce que ce sont principalement des lignes avec MATURITE nulle ?
mismatches_analyse = df_test.filter(pl.col('Ecart').abs() >= 0.001)
print(f"\nDont MATURITE nulle dans les mismatches : {mismatches_analyse['NB_MATURITE_IRBA'].null_count()}/{mismatches_analyse.height}")

# 3. Profil des 'autre' (écarts > 0.001 et NB_DURATION_IRBA != 1.0)
autres = df_test.filter(
    (pl.col('Ecart').abs() >= 0.001) & 
    (pl.col('NB_DURATION_IRBA') != 1.0)
)
print(f"\n=== ZOOM SUR 'AUTRE' ({autres.height} lignes) ===")
print(autres.select(['NB_MATURITE_IRBA', 'NB_DURATION_IRBA', 'Duration_Calculee', 'Ecart']).head(10))
