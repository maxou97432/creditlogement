"""
config.py - Configuration centrale pour indice_de_prix_insee.

Remplace config.yaml (qui causait des erreurs de parsing avec les
chemins Windows contenant des antislashs).

Modifier ici les chemins avant de lancer main.py.
"""
import os

# ---------------------------------------------------------------------------
# Répertoire racine du projet
# ---------------------------------------------------------------------------
PROJECT_DIR = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))

# ---------------------------------------------------------------------------
# DONNÉES — chemins à adapter selon l'environnement
# ---------------------------------------------------------------------------

# Dossier contenant les CSV de prix (produits par indicateur_prix/pipeline/calc_indice.py)
BASE_PATH = r"Z:\uflumth\gits\univers_elementaire\indicateur_prix\resultats\v1_complet"

# Dossier contenant le fichier INSEE (valeurs_trimestrielles.csv)
INSEE_PATH = r"Z:\uflumth\gits\univers_elementaire\TI\table_rapport (2025)"

# Année de référence pour le calcul des indices (base 100)
BASE_YEAR = 2020

# Noms des fichiers CSV à charger
FILES = {
    "city":     "prix_m2_univ_elem_complet_CITY.csv",
    "dept":     "prix_m2_univ_elem_complet_DEPT.csv",
    "region":   "prix_m2_univ_elem_complet_REGION.csv",
    "province": "prix_m2_univ_elem_complet_PROVINCE.csv",
    "insee":    "valeurs_trimestrielles.csv",
}

# Périmètres géographiques à conserver
PERIMETRES = [
    "77", "78", "91", "92", "93", "94", "95",          # Départements IDF
    "MARSEILLE", "LYON", "LILLE", "PARIS",               # Agglomérations
    "NORD-PAS-DE-CALAIS", "PROVENCE-ALPES-CÔTE D'AZUR", "RHÔNE-ALPES",
    "PROVINCE", "AUVERGNE", "ÎLE-DE-FRANCE",
]

# ---------------------------------------------------------------------------
# MODÈLE HOLT-WINTERS
# ---------------------------------------------------------------------------
HOLT_WINTERS = {
    "trend":                   "add",
    "seasonal":                None,        # Pas de saisonnalité
    "damped_trend":            True,
    "initialization_method":   "estimated",
    "min_observations":        8,
    "n_forecast":              2,
    "confidence_level":        0.95,
    "optimize_hyperparameters": True,
    "cv_splits":               3,
}

# ---------------------------------------------------------------------------
# INFLATION
# ---------------------------------------------------------------------------
INFLATION = {
    "method":           "compound",   # "simple" | "compound" | "exponential"
    "lookback_periods": 4,
    "smoothing_alpha":  0.3,
}

# ---------------------------------------------------------------------------
# TRAINING
# ---------------------------------------------------------------------------
TRAINING = {
    "retrain_frequency": "quarterly",
    "validation_split":  0.2,
    "min_train_size":    8,
    "save_criteria": {
        "min_correlation":        0.85,
        "max_rmse":               10.0,
        "improvement_threshold":  0.02,
    },
}

# ---------------------------------------------------------------------------
# INFERENCE
# ---------------------------------------------------------------------------
INFERENCE = {
    "confidence_level": 0.95,
    "output_format":    "csv",
    "batch_size":       100,
}

# ---------------------------------------------------------------------------
# CHEMINS DE SORTIE (relatifs au projet)
# ---------------------------------------------------------------------------
PATHS = {
    "models":      os.path.join(PROJECT_DIR, "artifacts", "models"),
    "predictions": os.path.join(PROJECT_DIR, "artifacts", "predictions"),
    "metrics":     os.path.join(PROJECT_DIR, "artifacts", "metrics"),
    "logs":        os.path.join(PROJECT_DIR, "logs"),
}

# ---------------------------------------------------------------------------
# MONITORING
# ---------------------------------------------------------------------------
MONITORING = {
    "metrics":         ["rmse", "correlation", "mean_absolute_error", "bias"],
    "drift_threshold": 0.15,
    "alert_email":     None,
}
