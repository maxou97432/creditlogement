for col in ["C_TYPE_AMORT", "cd_portfolio", "PERIM_PD"]:
    if col in base.columns:
        print(f"\n--- {col} ---")
        print(sous_pop2.group_by(["groupe", col]).agg(pl.len().alias("n")).sort(["groupe", col]))
