def ajouter_pivot_combine(workbook, df_stats_quali, df_stats_quanti,
                            sheet_name="Pivot_target_annee",
                            nom_feuille_quali="Stats_quali",
                            nom_feuille_quanti="Stats_quanti",
                            variable_defaut=None):
    variables_quali = sorted(df_stats_quali["variable"].dropna().unique())
    variables_quanti = sorted(df_stats_quanti["variable"].dropna().unique())
    toutes_variables = variables_quali + variables_quanti
    var_defaut = variable_defaut or (toutes_variables[0] if toutes_variables else "")

    annees = sorted(
        {a for a in df_stats_quali["annee"].unique() if pd.notna(a)}
        | {a for a in df_stats_quanti["annee"].unique() if pd.notna(a)}
    )

    modalites_par_var = {
        v: sorted(
            m for m in df_stats_quali.loc[df_stats_quali["variable"] == v, "modalite"].unique()
            if pd.notna(m)
        )
        for v in variables_quali
    }
    modalites_par_var.update({
        v: sorted(
            m for m in df_stats_quanti.loc[df_stats_quanti["variable"] == v, "modalite"].unique()
            if pd.notna(m)
        )
        for v in variables_quanti
    })
    nb_modalites_max = max((len(m) for m in modalites_par_var.values()), default=0)

    # ... reste de la fonction inchangé
