# Crée une colonne avec la différence de RWA entre Python et SAS
verification = result.with_columns(
    (pl.col('RWA_init').fill_null(0) - pl.col('RWA_Reboot').fill_null(0)).alias('diff_RWA')
)

# Filtre sur les lignes avec un gros écart et affiche les coupables
coupables = verification.filter(pl.col('diff_RWA').abs() > 1000)

print(f"Nombre de lignes avec gros écart : {coupables.height}")
print("\Aperçu des lignes coupables :")
print(coupables.select(
    ["duration", "RWA_init", "RWA_Reboot", "cd_portfolio", "EAD_IRBA", "diff_RWA"]
).sort("diff_RWA", descending=True).head(10))
