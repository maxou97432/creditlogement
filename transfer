# Liste TOUTES les colonnes du DataFrame brut, pas seulement celles testées
print("Toutes les colonnes du df :")
for c in sorted(df_avec_pred.columns):
    dtype = df_avec_pred.schema[c]
    n_unique = df_avec_pred[c].n_unique() if dtype in (pl.Utf8, pl.Categorical, pl.Int32, pl.Int64) else "—"
    print(f"  {c:<35} {str(dtype):<15} n_unique={n_unique}")
