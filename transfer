notebooks_orchestrateur = [{
    "name": nb["name"],
    "timeout": nb.get("timeout", 9000),
    "parameters": {
        "nom_notebook":  nb["name"],
        "chemin_entree": str(nb["input_path"])  if nb.get("input_path")  else None,
        "chemin_sortie": str(nb["output_path"]) if nb.get("output_path") else None,
        "run_id":        RUN_ID,
    },
} for nb in notebooks_actifs]

# Chemins RELATIFS : l'orchestrateur préfixe lui-même par OneDrive
config_run = {
    "pipeline_name": f"{config_brut['pipeline']['name']}_{RUN_ID}",
    "Working_path":  config_brut["working_path"],     # W majuscule, cf. ligne 88
    "notebooks_dir": config_brut["notebooks_dir"],
    "output_dir":    f"notebooks_executes/{RUN_ID}",
    "report_dir":    config_brut["report_dir"],
    "notebooks":     notebooks_orchestrateur,          # racine, cf. ligne 107
    "pipeline":      {"name": config_brut["pipeline"]["name"]},
    "execution":     config_brut["execution"],
}
