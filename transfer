n = filtered_lf_lgd.select(pl.len()).collect().item()
print(f"[{submodel_label}] lignes après filtre : {n}")
print("filtre appliqué :", modified_filter)
print(filtered_lf_lgd.select(
    pl.col("LGD").n_unique().alias("valeurs_distinctes"),
    pl.col("LGD").min().alias("min"),
    pl.col("LGD").max().alias("max"),
).collect())
