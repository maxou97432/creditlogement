# 1) Tester cette règle
pop = pop.with_columns(
    pl.when(
        ((pl.col("TX_CCF_IRBA_PNU") <= 0.27) & (pl.col("DT_DEBUT_AUTO") > 23276))
        | ((pl.col("TX_CCF_IRBA_PNU") > 0.27) & (pl.col("CD_OBJ_FIN_FA2") == 1))
    )
    .then(pl.col("NB_MATURITE_IRBA"))
    .otherwise(pl.col("NB_MATURITE_IRBA").clip(1.0, 5.0))
    .alias("NB_DURATION_SIMULE")
)
match = ((pop["NB_DURATION_SIMULE"] - pop["NB_DURATION_IRBA"]).abs() < 1e-6).sum()
print(f"Réplication : {match:,} / {len(pop):,}  ({100*match/len(pop):.3f}%)")

# 2) Confirmer la sémantique de CD_OBJ_FIN_FA2
print(
    df.group_by(["CD_OBJ_FIN_FA2", "LB_OBJ_FIN_Exportations"])
    .agg(pl.len().alias("N"))
    .sort("N", descending=True)
)

# 3) Vérifier la conversion dates SAS
import datetime
base = datetime.date(1960, 1, 1)
print("DT_DEBUT_AUTO 23276 ≈", base + datetime.timedelta(days=23276))
print("DT_ECHEANCE_FEM 24471 ≈", base + datetime.timedelta(days=24471))
