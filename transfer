# 1. Calculer la corrélation, désimbriquer l'index, et cibler la paire d'intérêt

# Renommons la colonne INSEE pour simplifier la cible
INSEE_COL = "Indice de prix (INSEE) - Base 2020"
CL_COL = "Indice de prix (CL)"

correlation_df = (
    merge.groupby(["VAL_MAILLE_GEO", "CD_TYPE_BIEN"])[
        [CL_COL, INSEE_COL]
    ]
    .corr()
    .unstack()
)

# 2. Extraire la seule colonne de corrélation croisée (CL vs INSEE)
# On cible la corrélation entre CL et INSEE.
correlation_mat = correlation_df[(CL_COL, INSEE_COL)]

# 3. Transformer l'Index en Colonnes régulières pour la fusion
correlation_mat = correlation_mat.reset_index()
# Le nom de la colonne de corrélation est maintenant 0 ou le nom de l'axe, il faut le renommer.
correlation_mat = correlation_mat.rename(columns={0: "Corrélation"})

# --- CORRECTION DU CALCUL DE CORRÉLATION ---
INSEE_COL = "Indice de prix (INSEE) - Base 2020"
CL_COL = "Indice de prix (CL)"

correlation_df = (
    merge.groupby(["VAL_MAILLE_GEO", "CD_TYPE_BIEN"])[[CL_COL, INSEE_COL]]
    .corr()
    .unstack()
)

# Extraction de la corrélation CL vs INSEE (doit être fait avant reset_index)
correlation_mat = correlation_df[(CL_COL, INSEE_COL)].reset_index() # MultiIndex Columns aplaties
correlation_mat = correlation_mat.rename(columns={correlation_mat.columns[-1]: "Corrélation"}) # Renommer la dernière colonne de corrélation

# --- Fusion finale (votre code original) ---
comparaison = (
    ecarts_stats
    .merge(rmse_results, on=["VAL_MAILLE_GEO", "CD_TYPE_BIEN"])
    .merge(correlation_mat, on=["VAL_MAILLE_GEO", "CD_TYPE_BIEN"])
)
