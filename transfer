print("classes avant :", df.select("CHR_model").n_unique())
print("groupes après :", mapping.select("merged_group").n_unique())
print(trace["decision"].value_counts())
print(f"eta² {avant:.6f} -> {apres:.6f} | perte relative {(avant-apres)/avant:.4%}")
