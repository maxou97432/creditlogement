
import pandas as pd
import numpy as np
import plotly.express as px
import plotly.io as pio
from statsmodels.tsa.holtwinters import ExponentialSmoothing
import warnings

# Configuration
warnings.filterwarnings("ignore")
pio.renderers.default = "notebook" 

# --- ÉTAPE 1 : Calcul de l'Indice Corrigé par HOLT (DES) ET Prévisions Futures ---

corrected_data = []
# Liste pour stocker les prévisions d'ajustement futur
future_adjustments = []

grouped = merge.groupby(["VAL_MAILLE_GEO", "CD_TYPE_BIEN"])

print("Calcul des modèles de Lissage de Holt par segment...")

for name, group in grouped:
    zone, type_bien = name
    group = group.sort_values("Periode").set_index("Periode")
    
    ts_ecart = group['Dif'].dropna()
    
    if len(ts_ecart) > 12: 
        try:
            # Modélisation Holt
            
            model_arima = ARIMA(train, order=(4, 1, 3), trend = 't') 
            fit_arima = model_arima.fit()



            # --- Partie A : Correction Historique ---
            ecart_modelise = fit_arima.fittedvalues
            ecart_modelise = ecart_modelise.reindex(group.index)
            group['Indice de prix (CL) - ARIMA'] = group['Indice de prix (CL)'] - ecart_modelise
            
            # --- Partie B : Prévision de l'Ajustement Futur ---
            # On prédit le biais pour T+1 et T+2
            forecast = fit_arima.forecast(2)
            
            future_adjustments.append({
                "VAL_MAILLE_GEO": zone,
                "CD_TYPE_BIEN": type_bien,
                "Ajustement_T+1": round(forecast.iloc[0], 2),
                "Ajustement_T+2": round(forecast.iloc[1], 2)
            })
            
        except Exception as e:
            print(f"Echec Holt pour {name}: {e}")
            group['Indice de prix (CL) - Corrigé Holt'] = np.nan
    else:
        group['Indice de prix (CL) - Corrigé Holt'] = np.nan
        
    corrected_data.append(group.reset_index())

# Reconstitution du DataFrame historique
merge_corrected = pd.concat(corrected_data, ignore_index=True)

# Création du DataFrame des prévisions futures
df_previsions = pd.DataFrame(future_adjustments)

# Affichage du tableau des ajustements (votre demande)
print("\n--- Tableau des Ajustements Futurs à Appliquer (Biais Prévu) ---")
print(df_previsions)


# --- ÉTAPE 2 : Préparation Plotly (Code inchangé pour vos graphiques) ---

df_plot = merge_corrected.melt(
    id_vars=["Periode", "VAL_MAILLE_GEO", "CD_TYPE_BIEN"],
    value_vars=[
        "Indice de prix (INSEE) - Base 2020", 
        "Indice de prix (CL)", 
        "Indice de prix (CL) - Corrigé Holt"
    ],
    var_name="Source",
    value_name="Indice_Valeur"
)

df_plot['Segment'] = df_plot['VAL_MAILLE_GEO'] + ' - ' + df_plot['CD_TYPE_BIEN']

# --- ÉTAPE 3 : Génération des Graphiques (Code inchangé) ---


segments_uniques = df_plot['Segment'].unique()

print(f"Génération des graphiques pour {len(segments_uniques)} segments...")

for segment in segments_uniques:
    df_segment = df_plot[df_plot['Segment'] == segment]
    df_segment = df_segment.dropna(subset=["Indice_Valeur"])
    
    if df_segment.empty:
        continue

    fig = px.line(
        df_segment,
        x="Periode",
        y="Indice_Valeur",
        color="Source",
        
        # Couleurs : Noir (Réalité), Rouge (Original), Bleu (Correction Holt)
        color_discrete_map={
            "Indice de prix (INSEE) - Base 2020": "black",
            "Indice de prix (CL)": "red",
            "Indice de prix (CL) - Corrigé Holt": "blue" 
        },
        
        title=f"Correction Dynamique (Holt) vs INSEE : {segment}",
        labels={
            "Indice_Valeur": "Indice (Base 2020)",
            "Periode": "Trimestre",
            "Source": "Indicateur"
        },
        height=500
    )

    # Styles de lignes pour bien distinguer
    # Pointillés pour l'original (CL) qui dérive
    fig.update_traces(patch={"line": {"dash": "dot", "width": 2}}, selector={"legendgroup": "Indice de prix (CL)"})
    # Trait épais solide pour la correction
    fig.update_traces(patch={"line": {"width": 3}}, selector={"legendgroup": "Indice de prix (CL) - Corrigé Holt"})
    # Trait solide normal pour l'INSEE
    fig.update_traces(patch={"line": {"width": 2}}, selector={"legendgroup": "Indice de prix (INSEE) - Base 2020"})

    # Zoom Y intelligent
    y_min = df_segment["Indice_Valeur"].min()
    y_max = df_segment["Indice_Valeur"].max()
    fig.update_yaxes(range=[y_min - 5, y_max + 5])

    # Ligne 100
    fig.add_hline(y=100, line_dash="dash", line_color="gray", opacity=0.5)

    fig.update_layout(template="plotly_white", title_x=0.5)
    fig.show()

{
	"name": "KeyError",
	"message": "\"The following id_vars or value_vars are not present in the DataFrame: ['Indice de prix (CL) - Corrigé Holt']\"",
	"stack": "---------------------------------------------------------------------------
KeyError                                  Traceback (most recent call last)
Cell In[43], line 74
     69 print(df_previsions)
     72 # --- ÉTAPE 2 : Préparation Plotly (Code inchangé pour vos graphiques) ---
---> 74 df_plot = merge_corrected.melt(
     75     id_vars=[\"Periode\", \"VAL_MAILLE_GEO\", \"CD_TYPE_BIEN\"],
     76     value_vars=[
     77         \"Indice de prix (INSEE) - Base 2020\", 
     78         \"Indice de prix (CL)\", 
     79         \"Indice de prix (CL) - Corrigé Holt\"
     80     ],
     81     var_name=\"Source\",
     82     value_name=\"Indice_Valeur\"
     83 )
     85 df_plot['Segment'] = df_plot['VAL_MAILLE_GEO'] + ' - ' + df_plot['CD_TYPE_BIEN']
     87 # --- ÉTAPE 3 : Génération des Graphiques (Code inchangé) ---

File n:\\uflumth\\indicateur_marche\\V2\\sklearn-env\\Lib\\site-packages\\pandas\\core\\frame.py:9969, in DataFrame.melt(self, id_vars, value_vars, var_name, value_name, col_level, ignore_index)
   9959 @Appender(_shared_docs[\"melt\"] % {\"caller\": \"df.melt(\", \"other\": \"melt\"})
   9960 def melt(
   9961     self,
   (...)   9967     ignore_index: bool = True,
   9968 ) -> DataFrame:
-> 9969     return melt(
   9970         self,
   9971         id_vars=id_vars,
   9972         value_vars=value_vars,
   9973         var_name=var_name,
   9974         value_name=value_name,
   9975         col_level=col_level,
   9976         ignore_index=ignore_index,
   9977     ).__finalize__(self, method=\"melt\")

File n:\\uflumth\\indicateur_marche\\V2\\sklearn-env\\Lib\\site-packages\\pandas\\core\\reshape\\melt.py:74, in melt(frame, id_vars, value_vars, var_name, value_name, col_level, ignore_index)
     70 if missing.any():
     71     missing_labels = [
     72         lab for lab, not_found in zip(labels, missing) if not_found
     73     ]
---> 74     raise KeyError(
     75         \"The following id_vars or value_vars are not present in \"
     76         f\"the DataFrame: {missing_labels}\"
     77     )
     78 if value_vars_was_not_none:
     79     frame = frame.iloc[:, algos.unique(idx)]

KeyError: \"The following id_vars or value_vars are not present in the DataFrame: ['Indice de prix (CL) - Corrigé Holt']\""
}
