import jenks_calculator as jc

# table préparée exactement comme le moteur l'attend
df_order = (
    lf.select(
        (100 - pl.col("LGD_3ANS_finale")).alias(jc._SCORE),
        pl.col("LGD_3ANS_finale").alias(jc._TARGET),
    )
    .group_by(jc._SCORE)
    .agg(
        pl.len().alias(jc._WEIGHT),
        pl.col(jc._TARGET).mean().alias(jc._TARGET_MEAN),
    )
    .sort(jc._SCORE)
    .collect()
    .to_pandas()
)
cache = jc._build_weighted_jenks_cache(df_order, 20)
print("k_feasible =", jc._max_possible_k(cache))
print("dp[1, -1] fini ?", np.isfinite(cache["dp"][1, -1]))
print("dp[2, -1] fini ?", np.isfinite(cache["dp"][2, -1]))
