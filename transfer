# combien de classes par segment a l'issue de Jenks ?
print(
    df_jenks
    .group_by("Submodel")          # ou la colonne qui identifie A-F
    .agg(
        pl.col("Jenks_class").n_unique().alias("nb_classes"),
        pl.len().alias("n_lignes"),
    )
    .sort("Submodel")
)
