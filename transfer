# TODO : reintroduire un vrai filtre par segment quand le pipeline sera
# generalise a plusieurs segments Cure/NonCure en parallele.
# Pour l'instant lf_cure_w_nc est deja restreint au segment courant en amont,
# donc pas besoin de refiltrer ici -- seg['Filtre'] est casse (is_in sur
# Int32) et n'est de toute facon plus necessaire dans ce contexte.
lf_cure_w_nc = lf_cure_w_nc.with_columns(
    pl.lit(mean_lgd_cure).alias('avg_LGD_Cure')
)
