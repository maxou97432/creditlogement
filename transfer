dtm.select([
    pl.col("Seg").null_count().alias("null_seg"),
    pl.col("HLC").null_count().alias("null_hlc"),
    pl.col("Seg_X_HLC").null_count().alias("null_cat"),
]).collect()

# volume concerne
dtm.filter(pl.col("Seg_X_HLC").is_null()).select(pl.len()).collect()
