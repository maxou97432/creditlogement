ovls = (cmp.detail()
        .filter(pl.col("test") == "OVL (Weitzman)")
        ["statistic"].to_numpy())

plt.hist(ovls, bins=20, edgecolor="black")
plt.axvline(cmp.seuil_calibre, color="green", ls=":", lw=2,
            label=f"seuil = {cmp.seuil_calibre}")
plt.xlabel("OVL")
plt.ylabel("nombre de paires")
plt.legend()
plt.show()
