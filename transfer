# Regarder des exemples d'outliers avec Non_specifie
print("=== EXEMPLES OUTLIERS NON_SPECIFIE ===")
non_spec = outliers[outliers["OUTLIER_REASON"] == "Non_specifie"]
print(f"Nombre: {len(non_spec)}")
print("\n5 exemples:")
print(non_spec[["ID", "Prix_m2", "NB_SURF_HAB", "NB_PIECES", "SOURCE", "OUTLIER_REASON"]].head(5))

print("\n=== STATS SUR CES OUTLIERS ===")
print(f"Prix_m2 est NaN: {non_spec['Prix_m2'].isna().sum()} / {len(non_spec)}")
print(f"NB_SURF_HAB est NaN: {non_spec['NB_SURF_HAB'].isna().sum()} / {len(non_spec)}")
print(f"NB_PIECES est NaN: {non_spec['NB_PIECES'].isna().sum()} / {len(non_spec)}")
