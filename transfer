from statsmodels.tsa.seasonal import seasonal_decompose
import matplotlib.pyplot as plt

# On prend un segment pour l'exemple (ex: PARIS - APT)
# Assurez-vous que ts_ecart est bien triée et a un index temporel (Trimestriel)
# ts_ecart.index.freq = 'Q' # Important pour statsmodels

print("Analyse de la Saisonnalité de l'Écart...")

try:
    # decomposition (model='additive' car l'écart peut être négatif)
    res = seasonal_decompose(ts_ecart, model='additive', period=4) # period=4 pour des trimestres

    # Affichage
    fig, (ax1, ax2, ax3, ax4) = plt.subplots(4, 1, figsize=(12, 10), sharex=True)
    
    res.observed.plot(ax=ax1, title='Observation (Écart Réel)')
    ax1.set_ylabel('Points')
    
    res.trend.plot(ax=ax2, title='Tendance (Trend)')
    ax2.set_ylabel('Points')
    
    res.seasonal.plot(ax=ax3, title='Saisonnalité Isolée')
    ax3.set_ylabel('Points')
    
    res.resid.plot(ax=ax4, title='Résidus (Bruit)')
    ax4.set_ylabel('Points')
    
    plt.xlabel('Date')
    plt.tight_layout()
    plt.show()

    # Test rapide de la force de la saisonnalité
    seasonal_range = res.seasonal.max() - res.seasonal.min()
    trend_range = res.trend.max() - res.trend.min()
    
    print(f"Amplitude de la saisonnalité : {seasonal_range:.4f} points")
    print(f"Amplitude de la tendance     : {trend_range:.4f} points")
    
    if seasonal_range > 0.2 * trend_range: # Règle empirique
        print("⚠️ ALERTE : Forte saisonnalité détectée !")
    else:
        print("✅ Pas de saisonnalité majeure détectée.")

except Exception as e:
    print(f"Impossible de décomposer (série trop courte ou index manquant) : {e}")
