regles = (base.group_by(keys).agg([
    pl.col("HLC").sort_by(EAD_COL, descending=True).first().alias("hlc_ead"),   # EAD dominant
    pl.col("HLC").mode().first().alias("hlc_majorite"),                          # majorite facilites
]))
desaccord = regles.filter(pl.col("hlc_ead") != pl.col("hlc_majorite"))
print(f"Couples ou EAD-dominant != majorite : {desaccord.height:,} "
      f"= {desaccord.height/tot*100:.1f}%")
