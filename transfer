DELTA_LGD = 1.0   # points de LGD — le seuil de matérialité, modifiable à la volée

def meilleur_candidat_fusion(
    df: pl.LazyFrame,
    nb_croisements_max: int = 0,      # conservés pour compat : ignorés
    ans_strict: int = 0,              # idem
    lgd_col: str = "lgd_obligor",
    jenks_col: str = "submodel_class",
    log: bool = False,
) -> Tuple[Optional[List[str]], Optional[pl.DataFrame]]:
    """
    Critère MATÉRIALITÉ (intra-segment) : fusionne la paire adjacente (par LRA)
    la plus proche dont l'écart de LGD moyenne est < DELTA_LGD.
    Signature et contrat de sortie IDENTIQUES à l'original (dbg expose
    'severite_totale' = -ecart_lgd, donc head(1) prend bien la plus proche).
    """
    ordre_global = (
        df.group_by([jenks_col, "year"])
        .agg(pl.col(lgd_col).mean().alias("mean_LGD_year"))
        .group_by([jenks_col])
        .agg(pl.col("mean_LGD_year").mean().alias("mean_LGD_global"))
        .with_columns(pl.col(jenks_col).str.split("_").list.first().alias("seg"))
        .sort(["seg", "mean_LGD_global"])
    )

    paires = (
        ordre_global.with_columns([
            pl.col(jenks_col).shift(-1).over("seg").alias("next_class"),
            pl.col("mean_LGD_global").shift(-1).over("seg").alias("next_lgd"),
        ])
        .filter(pl.col("next_class").is_not_null())
        .with_columns((pl.col("next_lgd") - pl.col("mean_LGD_global")).alias("ecart_lgd"))
        .filter(pl.col("ecart_lgd") < DELTA_LGD)
        .with_columns((-pl.col("ecart_lgd")).alias("severite_totale"))   # <<< plus proche = sévérité max
        .sort("severite_totale", descending=True)
        .select([jenks_col, "next_class", "severite_totale", "ecart_lgd"])
        .collect()
    )

    if log:
        print("=== Paires sous le seuil de matérialité ===")
        print("Aucune paire." if paires.height == 0 else paires)

    if paires.height == 0:
        return None, None

    meilleur = paires.head(1)
    return [meilleur[jenks_col][0], meilleur["next_class"][0]], paires

DELTA_LGD = 1.0   # changez ici pour tester d'autres seuils
df_fusionne_glouton, mapping_glouton = fusion_glouton(lra_prov_model3, log=True)

for d in [0.5, 1.0, 1.5, 2.0, 3.0, 5.0]:
    DELTA_LGD = d
    dff, _ = fusion_glouton(lra_prov_model3, log=False)
    n = dff.collect().select("submodel_class").n_unique()
    print(f"delta={d:>4}  ->  {n} classes")
