# Ces contrats sans CAD sont-ils d'un type de produit particulier ?
final_survival_rate.filter(
    pl.col("CAD_act__final").is_null() & pl.col("CRR__final").is_not_null()
).group_by("TYP_CPTE").agg(pl.len().alias("n")).sort("n", descending=True)
