import numpy as np

def m_bale_progressif(R, y, freq=12):
    n = max(1, int(round(R * freq)))
    if abs(y) < 1e-12:
        return (n + 1) / 2 / freq
    r = y / freq
    k = np.arange(1, n + 1)
    w = (1 + r) ** (k - 1)
    return float(np.sum(k * w) / np.sum(w)) / freq

# Population PROPRE des progressives : type 3, R > 1, non-amortissable = false
sub3 = df_avec_pred.filter(pl.col("CD_TYPE_AMORT") == 3).with_columns(
    has_171     = pl.col("FACTOR").str.contains(r"\[1\.71\]"),
    seg_special = pl.col("segment_trg").is_in([44, 73, 74]),
)
sub_prog = sub3.filter(
    (pl.col("R") > 1.0)
    & (~pl.col("has_171"))
    & (~pl.col("seg_special"))
)
print(f"Population progressive propre : n={sub_prog.height}")

R_arr  = sub_prog["R"].to_numpy()
NB_arr = sub_prog["NB_MATURITE_IRBA"].to_numpy()
y_arr  = sub_prog["T_INTRT"].to_numpy()

# 3 candidats
pred_5pct  = np.array([m_bale_progressif(r, 0.05) for r in R_arr])
pred_497   = np.array([m_bale_progressif(r, 0.0497) for r in R_arr])
pred_contr = np.array([m_bale_progressif(r, y) for r, y in zip(R_arr, y_arr)])

for name, pred in [("yield 5,00 %",      pred_5pct),
                    ("yield 4,97 %",     pred_497),
                    ("T_INTRT contract.", pred_contr)]:
    err = pred - NB_arr
    print(f"{name:<22} RMSE={np.sqrt(np.mean(err**2)):.5f}  "
          f"MAE={np.mean(np.abs(err)):.5f}  max={np.max(np.abs(err)):.5f}  "
          f"%<0.05={(np.abs(err)<0.05).mean()*100:.1f}%")

# Stratifié par tranche de R, pour voir si T_INTRT marche mieux sur certaines durées
print("\n--- Stratification par tranche de R ---")
print(f"{'tranche':<14} {'n':>8} {'RMSE 5%':>10} {'RMSE T_INTRT':>14}")
for lo, hi in [(1,3), (3,5), (5,10), (10,15), (15,20), (20,30), (30,60)]:
    mask = (R_arr >= lo) & (R_arr < hi)
    if mask.sum() < 50: continue
    rmse_5  = np.sqrt(np.mean((pred_5pct[mask]  - NB_arr[mask])**2))
    rmse_c  = np.sqrt(np.mean((pred_contr[mask] - NB_arr[mask])**2))
    print(f"[{lo:>2}, {hi:>2}[      {mask.sum():>8} {rmse_5:>10.5f} {rmse_c:>14.5f}")
