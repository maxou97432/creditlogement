from statsmodels.stats.diagnostic import acorr_ljungbox

# À ajouter après model_fit = model.fit() dans la boucle backtest
lb = acorr_ljungbox(model_fit.resid.dropna(), lags=[4, 8], return_df=True)
residus_ok = (lb["lb_pvalue"] >= 0.05).all()

if not residus_ok:
    print(f"   ⚠️ {zone}-{type_bien} : résidus encore autocorrélés (TES sous-spécifié)")
else:
    print(f"   ✅ {zone}-{type_bien} : résidus = bruit blanc")
