# base_merged = votre DataFrame avec les 2 bases jointes
problematic = base_merged[base_merged["RWA_init"].isna() & base_merged["RWA_Reboot"].notna()]

cols_to_check = [
    "duration", "tx_edf", "tx_lgd", "MT_EAD_FLOOR",
    "cd_portfolio", "perim", "HRC", "HLC"
]

# Comptage des nulls sur ces 56 lignes
print(problematic[cols_to_check].isna().sum())

# Répartition par cd_portfolio pour voir si c'est Retail ou non-Retail
print(problematic.groupby("cd_portfolio").size())
