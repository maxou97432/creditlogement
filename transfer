notebooks_orchestrateur = [{
    "name": nb["name"],
    "timeout": nb.get("timeout", 9000),
    "parameters": {
        "nom_notebook":  nb["name"],
        "chemin_entree": str(nb["input_path"])  if nb.get("input_path")  else None,
        "chemin_sortie": str(nb["output_path"]) if nb.get("output_path") else None,
        "run_id":        RUN_ID,
    },
} for nb in notebooks_actifs]

config_run = {
    "pipeline_name": f"{config['pipeline']['name']}_{RUN_ID}",
    "pipeline": {
        "name": config["pipeline"]["name"],
        "notebooks": notebooks_orchestrateur,
    },
    "execution":     config["execution"],
    "notebooks_dir": str(config["notebooks_dir"]),
    "output_dir":    str(config["output_dir"] / "notebooks_executes" / RUN_ID),
    "report_dir":    str(config["report_dir"]),
}

orchestrator = PipelineOrchestrator(config=config_run)
success = orchestrator.execute_pipeline()
