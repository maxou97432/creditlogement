# 1) La pire classe
pire = df_ob_class.sort("lgd_obligor_hlc", descending=True).head(3)
print(pire)
classe_ko = pire.select("Jenks_Submodel").row(0)[0]

# 2) Ce que contient RÉELLEMENT cette classe dans lf_LGD2
detail = (
    lf_LGD2
    .filter(pl.col("Jenks_Submodel") == classe_ko)
    .select(["Jenks_Submodel", config['Obligor'], "lgd_obligor"])
    .collect()
)
print("classe :", classe_ko)
print("nb lignes (facilités) :", detail.height)
print("nb obligors distincts :", detail.select(config['Obligor']).n_unique())
print(detail.select(
    pl.col("lgd_obligor").min().alias("lgd_min"),
    pl.col("lgd_obligor").max().alias("lgd_max"),
    pl.col("lgd_obligor").mean().alias("lgd_mean_attendu"),
    pl.col("lgd_obligor").is_null().sum().alias("nb_null"),
))
# counts a-t-il bien UNE seule ligne par classe ?
print("nb lignes dans counts :", counts.collect().height)
print("nb classes distinctes :", counts.collect().select("Jenks_Submodel").n_unique())
