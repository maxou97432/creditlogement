# ============================================================================
# CALIBRAGE MATÉRIALITÉ — niveau modèle
# ============================================================================
print("Calibrage seuil matérialité — niveau MODÈLE (avec diagnostic tailles d'effet)")
print("-" * 70)

meilleur_candidat_fusion = meilleur_candidat_fusion_modele

résultats = []
for d in [0.5, 0.75, 1.0, 1.5, 2.0, 2.5, 3.0]:
    DELTA_LGD_MODELE = d
    dff, _ = fusion_glouton(df_base, log=False)
    n_final = dff.collect().select(CLASS_COL).n_unique()
    résultats.append({"delta": d, "n_chr": n_final})
    print(f"  delta={d:>4}  ->  {n_final:>3} CHR_model")

print("\n" + "-" * 70)
print("Croisement avec TAILLES D'EFFET (du diagnostic):")
print("-" * 70)
print("d<0.5, OVL>0.3, W<2.0   -> 16 paires fusionnables -> ~49 CHR_model")
print("d<0.8, OVL>0.2, W<3.0   -> 25 paires fusionnables -> ~40 CHR_model")
print("d<1.0, OVL>0.15, W<4.0  -> 35 paires fusionnables -> ~30 CHR_model")
print("-" * 70)

df_res = pl.DataFrame(résultats)
print("\nMatérialité seule (pour comparaison) :")
print(df_res)
