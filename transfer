df_class = (
    df_obligor
    .group_by("Jenks_Submodel")
    .agg(
        pl.col("lgd_obligor").mean().alias("lgd_class"),
        pl.col("lgd_obligor").len().alias("n_obligor"),   # taille de classe = robustesse
    )
)

df_ob_class = df_class.sort("lgd_class", descending=True).collect()  # 1 LGD par classe
df_ob_class
