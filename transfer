srs_test = srs_fusion.with_columns(
    ((pl.col('DT_ECHEANCE_FEM') - DT_ARRT) / 365.25).alias('Test_Maturite_Residuelle'),
    ((pl.col('DT_ECHEANCE_FEM') - pl.col('DT_DEBUT_AUTO')) / 365.25).alias('Test_Maturite_Initiale'),
    ((DT_ARRT - pl.col('DT_DEBUT_AUTO')) / 365.25).alias('Test_Age_Credit')
)

# On vérifie avec la variable SAS (tolérance d'un jour = 0.01 an)
m_residuelle = srs_test.filter((pl.col('Test_Maturite_Residuelle') - pl.col('NB_MATURITE_IRBA')).abs() <= 0.01).height

print(f"Match Résiduel (Fin - Arrt) : {m_residuelle} sur {srs_test.height} lignes !")
