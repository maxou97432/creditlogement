df_mixtes = df_comparaison.filter(pl.col('CD_ENGAGEMENT').is_in(list(mixtes)))
df_mixtes_099 = df_mixtes.filter(pl.col('NB_MATURITE_IRBA') > 0.9)
df_mixtes_054 = df_mixtes.filter(pl.col('NB_MATURITE_IRBA') < 0.6)

print("Valeurs nulles dans le groupe 0.99 :")
print(df_mixtes_099.null_count().to_pandas().T)

print("\nValeurs nulles dans le groupe 0.54 :")
print(df_mixtes_054.null_count().to_pandas().T)
