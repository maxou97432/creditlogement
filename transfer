config_run = dict(config)
config_run["pipeline"] = {**config["pipeline"], "notebooks": notebooks_actifs}
config_run["run_id"] = RUN_ID
config_run["pipeline_name"] = f"{config['pipeline']['name']}_{RUN_ID}"

config_run = serialiser_pour_papermill(config_run)   # Path -> str

orchestrator = PipelineOrchestrator(config=config_run)
success = orchestrator.execute_pipeline()

print("\nRUN OK" if success else "\nRUN EN ÉCHEC")
