print(lf.select(
    pl.col(SCORE_COL).n_unique().alias("scores_distincts"),
    pl.col(TARGET_COL).n_unique().alias("lgd_distinctes"),
    pl.len().alias("n_lignes"),
).collect())
compute_jenks_run(filtered_lf, "score", "LGD", "year", seg_path)
import polars as pl
cols = lf.collect_schema().names()
print("colonnes:", cols)

# distribution de ce que tu passes en SCORE_COL (la colonne de découpe)
print(lf.select(
    pl.col(SCORE_COL).n_unique().alias("score_distincts"),
    pl.col(SCORE_COL).min().alias("score_min"),
    pl.col(SCORE_COL).max().alias("score_max"),
    pl.len().alias("n"),
).collect())
