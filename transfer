for scen_id in range(1, 5):
    lgd_scen = downturn.filter(pl.col("SCENARIO") == scen_id).select(["Segment", "LGD downturn"])
    base = base.join(
        lgd_scen,
        left_on=f"SCENARIO_{scen_id}",   # ← était modele_trg, doit être le label
        right_on="Segment",
        how="left"
    ).rename({"LGD downturn": f"LGD_DT_{scen_id}"})
