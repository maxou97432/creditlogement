import polars as pl

# Récupère les ids du sample (les 21 lignes)
ids = sample.select("id").to_series().to_list()

# Filtre la base brute et montre id + RWA
view = (
    base.filter(pl.col(INPUTS["id"]).is_in(ids))
        .select(
            pl.col(INPUTS["id"]).alias("id"),
            pl.col("cd_portfolio") if "cd_portfolio" in base.columns
                else pl.col(CD_PORTFOLIO).alias("cd_portfolio"),
            pl.col("RWA"),
            pl.col("RWA").list.len().alias("len"),
            pl.col("RWA").list.sum().alias("sum_RWA"),
            pl.col("RWA").list.first().alias("first_RWA"),
        )
        .sort("cd_portfolio")
)

print(view)
