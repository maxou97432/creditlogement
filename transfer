# DEBUG: Vérifier les colonnes disponibles
    print(f"\n  DEBUG - Colonnes dans result: {sorted(result.columns.tolist())}")
    print(f"\n  DEBUG - Vérification des colonnes attendues:")
    for col_source, dic_name in variables_mapping:
        exists = col_source in result.columns
        if exists:
            non_null = result[col_source].notna().sum()
            print(f"    ✓ {col_source}: {non_null} valeurs non-nulles")
        else:
            print(f"    ✗ {col_source}: MANQUANTE")
