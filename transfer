import pandas as pd
import numpy as np
import plotly.graph_objects as go
from statsmodels.tsa.holtwinters import ExponentialSmoothing
import plotly.io as pio
import warnings

# Configuration
warnings.filterwarnings("ignore")
pio.renderers.default = "notebook" 

corrected_data = [] 

# Groupement par zone et type de bien
grouped = merge.groupby(["VAL_MAILLE_GEO", "CD_TYPE_BIEN"])

print("🚀 Démarrage de la correction historique (Validation Holt-Winters)...")

for name, group in grouped:
    zone, type_bien = name
    
    # 1. Préparation des données
    # On s'assure que les dates sont l'index
    group = group.sort_values("Periode").set_index("Periode")
    
    # On initialise la colonne corrigée
    group['Indice de prix (CL) - Corrigé TES'] = np.nan
    
    # Série de l'écart (Biais)
    ts_ecart = group['Dif'].dropna()
    
    # Si besoin de forcer la fréquence (Trimestriel Début)
    if not ts_ecart.empty:
        try:
            ts_ecart.index.freq = 'QS'
        except:
            pass
    
    if len(ts_ecart) > 12:
        try:
            # 2. Modélisation TES (Holt-Winters)
            model = ExponentialSmoothing(
                ts_ecart, 
                trend='add', 
                seasonal='add', 
                seasonal_periods=4, 
                damped_trend=True, 
                initialization_method="estimated"
            )
            model_fit = model.fit()
            
            # 3. Calcul de la Correction Historique
            # On récupère les valeurs que le modèle a "lissées" sur le passé
            ecart_modelise = model_fit.fittedvalues
            
            # Alignement de sécurité sur l'index du groupe
            ecart_modelise = ecart_modelise.reindex(group.index)
            
            # APPLICATION DE LA CORRECTION
            # Indice Corrigé = Indice Brut - Biais Modélisé
            cl_corrected_history = group['Indice de prix (CL)'] - ecart_modelise
            group['Indice de prix (CL) - Corrigé TES'] = cl_corrected_history
            
            # 4. Visualisation (Historique pur)
            fig = go.Figure()

            # A. Historique INSEE (La Cible)
            fig.add_trace(go.Scatter(
                x=group.index, y=group['Indice de prix (INSEE) - Base 2020'],
                mode='lines', name='INSEE (Référence)',
                line=dict(color='black', width=3)
            ))

            # B. Historique CL Brut (Le Problème)
            fig.add_trace(go.Scatter(
                x=group.index, y=group['Indice de prix (CL)'],
                mode='lines', name='CL (Brut)',
                line=dict(color='red', width=1, dash='dot')
            ))

            # C. Historique CL Corrigé (La Solution)
            fig.add_trace(go.Scatter(
                x=group.index, y=group['Indice de prix (CL) - Corrigé TES'],
                mode='lines', name='CL Corrigé (TES)',
                line=dict(color='blue', width=2)
            ))

            fig.update_layout(
                title=f"<b>{zone} - {type_bien}</b> : Validation de la Correction",
                yaxis_title="Indice (Base 2020 = 100)",
                template="plotly_white",
                height=500,
                legend=dict(orientation="h", yanchor="bottom", y=1.02, xanchor="right", x=1)
            )
            
            fig.show()
            
        except Exception as e:
            print(f"⚠️ Echec TES pour {name} : {e}")
            
    corrected_data.append(group.reset_index())

# Reconstitution du DataFrame final avec les données corrigées
merge_corrected = pd.concat(corrected_data, ignore_index=True)
print("\nTraitement terminé. Les données corrigées sont dans 'merge_corrected'.")
