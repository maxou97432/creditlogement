import matplotlib.pyplot as plt
import geopandas as gpd # Très important pour lire le GeoJSON
from shapely.geometry import Point
# ... (vos autres imports) ...

# =============================================================================
# ÉTAPE 2 : CHARGEMENT DU CONTOUR DE LA FRANCE METROPOLITAINE
# =============================================================================

# Définissez le chemin vers votre fichier
geojson_path = r"chemin\vers\votre\metropole.geojson" # <-- **MODIFIEZ CE CHEMIN**

try:
    # 1. Charger la géométrie de la France (en WGS84 - EPSG:4326 par défaut)
    france_gdf = gpd.read_file(geojson_path)
    
    # 2. Simplifier l'accès à la géométrie (si le GeoJSON contient plusieurs polygones)
    # On prend la première (et souvent seule) géométrie pour le dessin
    france_outline = france_gdf.geometry.iloc[0] 
    
except Exception as e:
    print(f"Erreur lors du chargement du GeoJSON : {e}")
    # Gérer l'erreur si le fichier n'est pas trouvé ou invalide
    # Vous pouvez mettre ici une géométrie de secours si nécessaire

# --- NOTE IMPORTANTE SUR LA PROJECTION ---
# Pour un dessin précis, il est conseillé de travailler en Lambert 93 (EPSG:2154).
# Si vous avez des problèmes de distorsion, reprojetez comme ceci :
# france_crs = "EPSG:2154" 
# france_outline = france_gdf.to_crs(france_crs).geometry.iloc[0]
# gdf = gdf.to_crs(france_crs) # Reprojeter aussi vos points de villes !

# Dessiner le fond de la France
# Utiliser les coordonnées brutes (xy) de l'objet Shapely
ax.plot(france_outline.exterior.xy[0], france_outline.exterior.xy[1], color='black', linewidth=1.5) # Contour
ax.fill(france_outline.exterior.xy[0], france_outline.exterior.xy[1], color='rgb(255, 223, 110)') # Remplissage jaune

# Définir les limites de la carte
ax.set_xlim(france_outline.bounds[0] - 0.5, france_outline.bounds[2] + 0.5)
ax.set_ylim(france_outline.bounds[1] - 0.5, france_outline.bounds[3] + 0.5)

# ... (Le reste du code pour les étiquettes et les bulles reste inchangé) ...
