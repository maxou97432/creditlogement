from utils.stat_desc import write_formatted_sheet

if full_correlation is not None:
    write_formatted_sheet(writer, full_correlation, 'Full_correlation', '#8E44AD', index=True)
if stats_quali is not None:
    write_formatted_sheet(writer, stats_quali, 'Stats_quali', '#006C44')
if stats_quanti is not None:
    write_formatted_sheet(writer, stats_quanti, 'Stats_quanti', '#006C44')
