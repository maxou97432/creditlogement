print("DEBUG — entree cherchée   :")
print(repr(entree))
print(repr(entree.resolve()))
print("DEBUG — sorties prévues (une par ligne) :")
for s in sorted(sorties_prevues_ce_run, key=str):
    print(repr(s))
