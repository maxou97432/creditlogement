MARGIN_MODE = "absolute"
DELTA_LGD = 1.5
MAX_ITER = 1000        # large, juste une sécurité, pas une vraie limite

lf = df_base.lazy()
n0 = lf.select(CLASS_COL).unique().collect().height
reduced_lf, history = iterative_homogeneity_reduction(lf, max_iter=MAX_ITER)
df_final = reduced_lf.collect()
n1 = df_final[CLASS_COL].n_unique()

print(f"δ={DELTA_LGD} → {n0} à {n1} classes en {len(history)} fusions")
hist_df = pl.DataFrame(history)
with pl.Config(tbl_rows=100, tbl_cols=-1):
    print(hist_df.select(["iteration","class_a","class_b","gap","n_a","n_b","reason"]))
