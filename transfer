outliers = dtm.filter(pl.col("CAD_act_14ans") < -10)  # adapte l'horizon si besoin
print(
    outliers.group_by("Seg_X_HLC")
    .agg(pl.len().alias("n_outliers"))
)

# Et la taille totale de chaque classe concernée
class_sizes = dtm.group_by("Seg_X_HLC").agg(pl.len().alias("n_total")).collect()
print(class_sizes.sort("n_total"))
