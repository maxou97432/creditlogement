srs_X_capret3 = srs_X_capret.filter(pl.col('CD_TYPE_AMORT') == '3')

# Formule analytique Type 3 : M = (N+1)/2, N = nombre de mois restants
srs_X_capret3 = srs_X_capret3.with_columns(
    ((pl.col('MATURITE') + 1) / 2 / 12).alias('MATURITE_CALCULEE')
)

# Comparaison
n = srs_X_capret3.filter(
    (pl.col('MATURITE_CALCULEE') - pl.col('NB_MATURITE_IRBA')).abs() < 0.1
).height
print(f"Match Type 3 avec (MATURITE+1)/2/12 : {n}/{srs_X_capret3.height} ({n/srs_X_capret3.height:.1%})")

# Statistiques de l'écart résiduel
print(srs_X_capret3['MATURITE_CALCULEE'].alias('calc').describe())
print((srs_X_capret3['MATURITE_CALCULEE'] - srs_X_capret3['NB_MATURITE_IRBA']).describe())
