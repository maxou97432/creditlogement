positive_cases = final_survival_rate.filter(pl.col("CAD_act__final") > 0)
print(
    positive_cases.group_by(category_column)
    .agg(pl.len().alias("n_positive"))
    .sort("n_positive", descending=True)
)
