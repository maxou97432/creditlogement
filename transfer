import numpy as np

# 1) Restreindre à Corporate uniquement
corp = df_pl[df_pl["CD_PORTFOLIO"] == "Corporate"].copy()
print(f"Corporate : {len(corp):,}")

# 2) Stats robustes (médiane, pas moyenne)
print("\n>>> Delta Corporate (stats robustes)")
print(corp["delta"].describe(percentiles=[.1, .25, .5, .75, .9, .95, .99]))

# 3) Répartition par ordres de grandeur
bins = [0, 0.001, 0.01, 0.1, 0.5, 1, 5, 100, np.inf]
print("\n>>> Répartition du delta Corporate")
print(corp["delta"].dropna().pipe(
    lambda s: pd.cut(s, bins).value_counts().sort_index()
))

# 4) Où explose-t-il ?
extremes = corp.nlargest(20, "delta")[
    ["PD_IRBA", "LGD_IRBA", "EAD_IRBA", "RWA_IRBA",
     "NB_MATURITE_IRBA", "NB_DURATION_IRBA", "M_implied", "delta"]
]
print("\n>>> 20 plus grosses valeurs de delta")
print(extremes)

# 5) Distribution de PD dans les extrêmes vs normaux
print("\n>>> PD dans les deltas > 1 vs deltas < 0.01")
print("  extrêmes :", corp[corp["delta"]>1]["PD_IRBA"].describe())
print("  OK       :", corp[corp["delta"]<0.01]["PD_IRBA"].describe())
