# ============================================================================
# ENREGISTREMENT : appliquer la fusion au delta optimal et sauver
# ============================================================================

# Fixez votre delta final (à partir du graphique du coude)
DELTA_LGD_MODELE = 1.5   # ← votre choix, par exemple

meilleur_candidat_fusion = meilleur_candidat_fusion_modele

# Lancez la fusion sur base
df_fusionne_final, mapping_final = fusion_glouton(df_base, log=True)

# Collectez et vérifiez
df_fusionne_collected = df_fusionne_final.collect()
n_final = df_fusionne_collected.select(CLASS_COL).n_unique()
print(f"\n✓ Fusion complète : {n_final} CHR_model finales avec delta={DELTA_LGD_MODELE}")

# Enregistrez en parquet (format compressé, standard BNP)
output_path = "/mnt/user-data/outputs/base_fusionnee_chr_model.parquet"
df_fusionne_collected.write_parquet(output_path)
print(f"✓ Fichier sauvegardé : {output_path}")

# (Optionnel) Enregistrez aussi le mapping classe_avant → classe_après, 
# si vous avez besoin de traçabilité pour le validateur
# mapping_final : c'est ce que fusion_glouton retourne en 2ᵉ élément
if mapping_final is not None:
    mapping_final.write_parquet(
        "/mnt/user-data/outputs/mapping_chr_model_before_after.parquet"
    )
    print(f"✓ Mapping avant/après sauvegardé")

# Vérification rapide : comparez avant/après
print("\n" + "=" * 70)
print("VÉRIFICATION AVANT/APRÈS :")
print("=" * 70)
print(f"Classes avant fusion (CHR initial)  : {df_base.select(CLASS_COL).n_unique()}")
print(f"Classes après fusion                : {n_final}")
print(f"Réduction                           : {df_base.select(CLASS_COL).n_unique() - n_final} classes fusionnées")
print(f"Taux de réduction                   : {100 * (1 - n_final / df_base.select(CLASS_COL).n_unique()):.1f} %")
