import polars as pl

print("\n--- Échantillon Groupe 1 (Linéaire) ---")
df_groupe1 = (
    dataset_valide.filter(pl.col('CD_TYPE_AMORT') == '1')
    .select(['MATURITE', 'T_INTRT', 'NB_DURATION_IRBA'])
    .drop_nulls()
    .head(10)
)
print(df_groupe1)

print("\n--- Échantillon Groupe 3 (Progressif) ---")
df_groupe3 = (
    dataset_valide.filter(pl.col('CD_TYPE_AMORT') == '3')
    .select(['MATURITE', 'T_INTRT', 'NB_DURATION_IRBA'])
    .drop_nulls()
    .head(10)
)
print(df_groupe3)
