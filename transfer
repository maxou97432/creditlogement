lf = pl.scan_parquet(OUTPUT_DIR / config["RDS"])

# Liste des risk drivers depuis l'Excel de référence
rd = pd.read_excel(RD_PATH)
colonnes_presentes = lf.collect_schema().names()

# ⚠️ à adapter aux noms réels des colonnes de l'Excel RD
COL_NOM  = "Variable"
COL_TYPE = "Type"

rd = rd[rd[COL_NOM].isin(colonnes_presentes)]
drivers_quali  = rd.loc[rd[COL_TYPE].str.upper().str.startswith("QUAL"), COL_NOM].tolist()
drivers_quanti = rd.loc[rd[COL_TYPE].str.upper().str.startswith("QUAN"), COL_NOM].tolist()

print(f"{len(drivers_quali)} quali | {len(drivers_quanti)} quanti "
      f"| total {len(drivers_quali) + len(drivers_quanti)}")

# Colonne année dérivée de la date photo
lf = lf.with_columns(pl.col(DATE_PHOTO).dt.year().alias("annee"))
