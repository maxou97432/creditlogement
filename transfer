# Zoom sur C_EST_PERIODE
print("=== C_EST_PERIODE ===")
print(capret['C_EST_PERIODE'].value_counts().sort('count', descending=True))

# Et DT_ECHEANCE_AUTO_MOY (mentionné plus tôt)
print("\n=== DT_ECHEANCE_AUTO_MOY ===")
if 'DT_ECHEANCE_AUTO_MOY' in capret.columns:
    print(f"Min: {capret['DT_ECHEANCE_AUTO_MOY'].min()}")
    print(f"Max: {capret['DT_ECHEANCE_AUTO_MOY'].max()}")
    print(f"Null: {capret['DT_ECHEANCE_AUTO_MOY'].null_count()}")

# Colonnes numériques entières qui ressemblent à des jours (100 à 36500)
print("\n=== CANDIDATS initial_maturity_days ===")
for c in capret.columns:
    if capret[c].dtype in [pl.Int32, pl.Int64]:
        mn, mx = capret[c].min(), capret[c].max()
        if mn is not None and 100 < mn and mx < 36500:
            print(f"{c}: min={mn}, max={mx}")
