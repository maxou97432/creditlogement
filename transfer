import yaml
from pathlib import Path

config_source = {
    **CHEMINS,
    **FICHIERS_SOURCES,
    **FICHIERS_INTERMEDIAIRES,
    **VARIABLES_METIER,
    "pipeline": {"name": "Pipeline_LGD_RiskQuant", "notebooks": CATALOGUE_NOTEBOOKS},
    "execution": EXECUTION,
}

with open("config.yaml", "w", encoding="utf-8") as f:
    yaml.dump(config_source, f, sort_keys=False, allow_unicode=True)

print("config.yaml régénéré à partir des constantes de Run Pipeline.ipynb")
