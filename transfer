# --- TEST DE VÉRIFICATION POUR NICE ---
# Transactions qui étaient initialement dans Nice (code INSEE 06088)
code_insee_nice_ville = "06088" 
mask_nice_ville_seule = base_ml["IRIS"].str[:5] == code_insee_nice_ville
df_nice_ville = base_ml[mask_nice_ville_seule].copy()

# Transactions qui étaient dans l'agglomération mais PAS dans la ville de Nice
# On retire le code 06088 de la liste des codes de l'agglomération pour isoler les périphéries
codes_peripheriques = [c for c in codes_insee_nice if c != code_insee_nice_ville]
mask_peripherie = base_ml["IRIS"].str[:5].isin(codes_peripheriques)
df_nice_peripherie = base_ml[mask_peripherie].copy()

print("\n--- Analyse des prix Nice vs Périphérie (Toutes périodes) ---")
print(f"Volume Nice Ville (06088): {len(df_nice_ville):,} transactions. Prix moyen: {df_nice_ville['Prix_m2'].mean():.2f} €/m²")
print(f"Volume Nice Périphérie: {len(df_nice_peripherie):,} transactions. Prix moyen: {df_nice_peripherie['Prix_m2'].mean():.2f} €/m²")
print("----------------------------------------------------------\n")
