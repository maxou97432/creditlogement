if config.get("run_stat_desc", False):
    dossier_stat_desc = OUTPUT_DIR / "stat_desc"
    full_correlation = pl.read_parquet(dossier_stat_desc / "full_correlation.parquet").to_pandas()
    stats_quali = pl.read_parquet(dossier_stat_desc / "stats_quali.parquet").to_pandas()
    stats_quanti = pl.read_parquet(dossier_stat_desc / "stats_quanti.parquet").to_pandas()
    trend_df = pl.read_parquet(dossier_stat_desc / "trend_modalities.parquet").to_pandas()
else:
    full_correlation = stats_quali = stats_quanti = trend_df = None
