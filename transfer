import importlib
import tests_heterogeneite as th
importlib.reload(th)
from tests_heterogeneite import calibrer_seuil_ovl

# sweep complet (~2 minutes : OVLTest est déterministe, pas de bootstrap)
df_calibration = calibrer_seuil_ovl(
    df_brut,
    class_col="CHR_apres_fusion",
    lgd_col="lgd_obligor",
    year_col="year",
    seg_col="Seg",
    obligor_col="ID_CLIENT",
    poids_temporel=0.5,
    poids_segment=0.5,
)
print(df_calibration)

# seuil optimal = celui qui minimise le score composite
seuil_optimal = float(
    df_calibration
    .filter(pl.col("score_composite") == pl.col("score_composite").min())
    ["seuil_ovl"][0]
)
print(f"\nSeuil OVL retenu : {seuil_optimal}")
