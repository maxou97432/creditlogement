import polars as pl

# Remplacez 'srs_fusion' par le nom de votre DataFrame actuel
df_comparaison = srs_fusion.filter(
    (pl.col('CD_TYPE_AMORT') == '3') &
    (pl.col('DT_ECHEANCE_FEM') == 24287.0)
)

# On sépare les deux groupes 
df_099 = df_comparaison.filter(pl.col('NB_MATURITE_IRBA') > 0.9)
df_054 = df_comparaison.filter(pl.col('NB_MATURITE_IRBA') < 0.6)

# On affiche un exemple de chaque pour chercher la colonne qui diffère
print("========== LIGNES À 0.9993 (Mode Sécurité) ==========")
print(df_099.head(2).to_pandas().T) 

print("\n========== LIGNES À 0.5448 (Mode Calcul) ==========")
print(df_054.head(2).to_pandas().T)

# Si vous préférez fouiller dans Excel, décommentez cette ligne :
# df_comparaison.write_excel('comparaison_anomalie.xlsx')
