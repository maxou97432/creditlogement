    # ✅ DEBUG : Vérifier les données
    print(f"\n[DEBUG] res_v2_struct shape: {res_v2_struct.shape}")
    print(f"[DEBUG] res_v2_struct colonnes: {res_v2_struct.columns.tolist()}")
    if len(res_v2_struct) > 0:
        print(f"[DEBUG] Periodes min/max: {res_v2_struct['Periode'].min()} / {res_v2_struct['Periode'].max()}")
    
    # ✅ Préparer une copie pour la carte (dernière période uniquement)
    res_v2_map = res_v2_struct[res_v2_struct["Periode"] == res_v2_struct["Periode"].max()].copy()
    
    print(f"[DEBUG] res_v2_map shape après filtre: {res_v2_map.shape}")
    
    # ✅ Si pas de données, on ne peut pas continuer
    if len(res_v2_map) == 0:
        print("⚠️ ERREUR: Aucune donnée pour la carte ! Vérifiez les données V2.")
        # On garde l'ancien système
        df_v2 = load_cl_results(PATH_RESULTATS_V2, "V2")
        if df_v2 is None:
            sys.exit("Erreur: Pas de données V2.")
        prix_m2_univ_elem_MAJ_CITY = df_v2[df_v2["MAILLE_TYPE"] == "CITY"].copy()
    else:
        # ✅ Adapter la structure pour la carte
        res_v2_map = res_v2_map.rename(columns={
            "Periode": "DT_MAJ_PM2",
            "Ville": "VAL_MAILLE_GEO",
            "prix_mm_12m (V2_STRUCT)": "prix_mm_value"
        })
        res_v2_map["MAILLE_TYPE"] = "CITY"  # Ajouter la colonne manquante
        res_v2_map["CD_TYPE_BIEN"] = "APT"  # Type de bien
        res_v2_map["DIC_NAME"] = "prix_m2_12m"  # Nom du dictionnaire

        # A. Utiliser res_v2_map pour la carte
        df_v2 = res_v2_map.copy()
        prix_m2_univ_elem_MAJ_CITY = df_v2[df_v2["MAILLE_TYPE"] == "CITY"].copy()
