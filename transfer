# 1. Combien de classes/générations sont concernées
bad = final_survival_rate.filter(pl.col("CAD_act__final") > 0)
print(
    bad.group_by([category_column, "generation"])
    .agg(pl.len().alias("n_cases"))
    .sort("n_cases", descending=True)
)

# 2. Est-ce concentré sur peu de couples classe/génération (hypothèse 2)
#    ou étalé partout (hypothèse 1, dérive générale du mécanisme) ?
print(bad.select([category_column, "generation"]).n_unique())
