# Vérifier : est-ce que IND_CREDIT_CONFIRME=0 + MATURITE<1 = les anomalies ?
regle_candidate = base.filter(
    (pl.col("IND_CREDIT_CONFIRME") == 0.0) &
    (pl.col("NB_MATURITE_IRBA") < 1.0)
)

print(f"Prédit par la règle IND_CREDIT_CONFIRME=0 + MATURITE<1 : {regle_candidate.height}")
print(f"Anomalies réelles                                       : {groupe_anomalie.height}")

# Chevauchement
match = base.filter(
    (pl.col("NB_DURATION_IRBA").round(3) != pl.col("NB_MATURITE_IRBA").clip(1.0, 5.0).round(3)) &
    (pl.col("IND_CREDIT_CONFIRME") == 0.0)
).height
print(f"Anomalies expliquées par IND_CREDIT_CONFIRME=0 : {match}/{groupe_anomalie.height}")
