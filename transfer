# --- CORRECTION : Utilisation directe des données en mémoire ---
# On évite de relire le fichier CSV pour ne pas avoir d'erreur de chemin

if 'res_villes' in locals() and res_villes is not None:
    # 1. On prend le résultat calculé juste avant
    df_temp = res_villes.copy()
    
    # 2. On ne garde que la dernière date (comme le fait le fichier MAJ)
    last_date = df_temp["Periode"].max()
    prix_m2_univ_elem_MAJ_CITY = df_temp[df_temp["Periode"] == last_date].copy()
    
    # 3. On renomme la colonne pour que la suite du code (carte) fonctionne
    prix_m2_univ_elem_MAJ_CITY = prix_m2_univ_elem_MAJ_CITY.rename(columns={"Periode": "DT_MAJ_PM2"})
    
    print(f"Données pour la carte chargées depuis la mémoire (Date : {last_date})")

else:
    print("ERREUR CRITIQUE : Les résultats 'res_villes' sont vides. Impossible de générer la carte.")
    # Fallback de secours si tu veux vraiment lire le fichier (avec le bon chemin dynamique)
    try:
        path_csv = os.path.join(CONFIG['output_dir'], "prix_m2_univ_elem_MAJ_CITY.csv")
        prix_m2_univ_elem_MAJ_CITY = pd.read_csv(path_csv)
    except:
        sys.exit("Arrêt : Fichier introuvable et pas de données en mémoire.")
