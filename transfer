srs_X_capret_test = srs_X_capret.with_columns(
    (pl.col('MATURITE') / 12.0).clip(1.0, 5.0).alias('Duration_depuis_MATURITE')
)

n = srs_X_capret_test.filter(
    (pl.col('Duration_depuis_MATURITE') - pl.col('NB_DURATION_IRBA')).abs() < 0.001
).height
print(f"clip(MATURITE/12, 1, 5) vs NB_DURATION_IRBA : {n}/{srs_X_capret_test.height} ({n/srs_X_capret_test.height:.1%})")

# Et pour comprendre la différence avec clipper la maturité Bâle directement
n2 = srs_X_capret_test.filter(
    (pl.col('duration') - pl.col('NB_DURATION_IRBA')).abs() < 0.001
).height
print(f"duration (capret, déjà clippée) vs NB_DURATION_IRBA : {n2}/{srs_X_capret_test.height} ({n2/srs_X_capret_test.height:.1%})")
