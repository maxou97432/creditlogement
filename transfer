
""" Ce fichier a pour but de créer et traiter la deuxième version de la base de données
    On reprend la bdd base_complete_filtree.parquet;
    On ne garde ensuite que les données de Crédit Logement, issues de France héxagonale, 
    et on retire les transactions RAC et TVX
    La base obtenue se trouve dans resultats.
"""
import sys
import os
import time
import logging
import pandas as pd
import numpy as np
import plotly.express as px
import plotly.io as pio
from fonctions import Connector
from fonctions import load_and_preprocess_data, save_results, filter_villes_top
import param_generaux as params

pio.renderers.default = "browser"  
# Configuration de l'environnement
current_dir = os.getcwd()
parent_dir = os.path.abspath(os.path.join(current_dir, '..'))
sys.path.append(r"N:\uflumth\indicateur_marche\univers_elementaire\TI\resultats")



# Configuration des dossiers
OUTPUT_DIR = os.path.join(os.path.dirname(__file__), "resultats")
os.makedirs(OUTPUT_DIR, exist_ok=True)

DATA_PATH = r"Z:\data\cl_estim\cleaned_data"

def add_metric(metrics, step, before, after, note=""):
    metrics.append({
        "étape": step,
        "lignes_avant": int(before),
        "lignes_après": int(after),
        "retirées": int(before - after),
        "note": note
    })

# --- 0. Init ---
t0 = time.time()
metrics = []
co = Connector()

# --- 1. IMPORTATION ---
base_clean = pd.read_parquet(r"Z:\uflumth\gits\univers_elementaire\TI\stats_base_complete_filtree\base_complete_filtree.parquet")





# --- 2. RESTRICTIONS DU PÉRIMÈTRE D'ÉTUDE ---

# A. Source = CL
before = len(base_clean)
mask_source = base_clean["SOURCE"].eq("CL")
cl_data = base_clean[mask_source].copy()
add_metric(metrics, "Filtre SOURCE = CL", before, len(cl_data))

# B. Métropole (01-95 + 2A/2B)
before = len(cl_data)
dept = cl_data["DEPT_Corse_2A_2B"].astype(str).str.upper().str.strip()
metropole_set = {f"{i:02d}" for i in range(1, 96)} | {"2A", "2B"}
mask_metro = dept.isin(metropole_set)
cl_data = cl_data[mask_metro].copy()
cl_data = cl_data.rename(columns={"DEPT_Corse_2A_2B":"DEPT"})
add_metric(metrics, "Filtre Géographie (métropole)", before, len(cl_data))

# C. Nature d'opération : exclure TVX et RAC
before = len(cl_data)
cl_data["CD_NATUR_OP"] = cl_data["CD_NATUR_OP"].fillna("Non renseigné")
mask_valid_op = ~cl_data["CD_NATUR_OP"].isin(["TVX", "RAC"])
cl_data = cl_data[mask_valid_op].copy()
add_metric(metrics, "Filtre Nature d’opération (excl. TVX/RAC)", before, len(cl_data))

# --- 3. FILTRE TEMPOREL ---
before = len(cl_data)
cl_data["DATE"] = cl_data["DATE"].apply(lambda x: x.replace(day=1))
cl_data["DATE"] = pd.to_datetime(cl_data["DATE"], errors="coerce")
DATE_MIN = pd.Timestamp("2014-01-01")
DATE_MAX = cl_data["DATE"].max()  # ou date précise à choisir
mask_date = (cl_data["DATE"] >= DATE_MIN) & (cl_data["DATE"] <= DATE_MAX)
base_ml = cl_data[mask_date].copy()
add_metric(metrics, "Filtre Date (>= 2014-01-01, < DATE_MAX)", before, len(base_ml))

resume_operation = pd.DataFrame(metrics)
print(resume_operation)
# =============================================================================
# 4. EXPORT
# =============================================================================
colonnes_finales = ["ID", "ville_","code_iris", "LIBELLE_DEPT", "DEPT", 
                        "TYPE_BIEN", "DATE", "YEAR", "Prix_m2", "SOURCE","VALEUR_FONCIERE",
                        "CD_NATUR_OP", "NB_SURF_HAB", "NB_PIECES_RANGE", 
                        "CODE_POSTAL", "BIEN_NEUF","DEMANDE"]

base_ml = base_ml[colonnes_finales]
output_path = os.path.join(OUTPUT_DIR, "base_restreinte.parquet")
base_ml.to_parquet(output_path, index=False)
print(f"\n✓ Fichier sauvegardé : {output_path}")

analyse = base_ml[["DATE","Prix_m2","NB_SURF_HAB","NB_PIECES_RANGE",'VALEUR_FONCIERE']]
# Statistiques globales sur colonnes numériques
("STATISTIQUES NUMÉRIQUES")
print("-" * 80)
print(analyse.describe())

# Médiane et quartiles
print("\nMédianes et quartiles :")
for col in ["Prix_m2", "NB_SURF_HAB", "NB_PIECES","VALEUR_FONCIERE"]:
    if col in analyse.columns:
        print(f"\n{col}:")
        print(f"  Médiane     : {analyse[col].median():,.0f}")
        print(f"  Q1 (25%)    : {analyse[col].quantile(0.25):,.0f}")
        print(f"  Q3 (75%)    : {analyse[col].quantile(0.75):,.0f}")
        print(f"  Écart-type  : {analyse[col].std():,.0f}")

# La suite du fichier permet de calculer les prix des différentes mailles géographiques selon la méthode des univers élémentaires
# Ici on applique les poids structurels issus de cette base de données
# Dans comparaison V1 vs V2.py, on applique les poids de la base originale DVF + CL à cette base restreinte


if False:
    # =============================================================================
    # 5. CALCUL DES PRIX V2_STRUCT (avec fonctions.py)
    # =============================================================================
    print("\n" + "="*80)
    print(" CALCUL DES PRIX V2_STRUCT (Poids V1 sur Base Restreinte)")
    print("="*80)

    path_base_v1 = r"N:\uflumth\indicateur_marche\univers_elementaire\base_complete_filtree.parquet"
    path_base_v2 = output_path

    df_v1 = load_and_preprocess_data(specific_path=path_base_v1)
    df_v1 = df_v1[df_v1["DEMANDE"] == 0]
    df_v1 = filter_villes_top(df_v1)
    structure_v1 = get_structure_reference(df_v1, maille_col="CITY")

    df_v2 = load_and_preprocess_data(specific_path=path_base_v2)

    res_v2_struct = process_maille(
        df=df_v2,
        maille_col="CITY",
        maille_name="CITY_V2_STRUCT",
        filter_func=filter_villes_top,
        df_structure_ref=structure_v1
    )

    save_results(res_v2_struct, "CITY_RESTREINT")
    print("\n✓ Prix V2_STRUCT calculé et sauvegardé")


