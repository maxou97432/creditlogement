# Cellule 1 - reload + imports
import importlib
import polars as pl
import tests_heterogeneite as th
importlib.reload(th)
from tests_heterogeneite import (
    FusionComparator, OVLTest, OVLPermutationTest, calibrer_seuil_ovl,
)

# Cellule 2 - run() une seule fois (~30 sec)
cmp = FusionComparator(
    df_brut,
    class_col="CHR_apres_fusion",
    lgd_col="lgd_obligor",
    obligor_col="ID_CLIENT",
    pairs="adjacent",
    tests=[OVLTest(method="kde")],
).run()

# Cellule 3 - calibration sur le cache (~2 min pour 10 seuils)
df_calibration = calibrer_seuil_ovl(
    cmp=cmp,
    df=df_brut,
    class_col="CHR_apres_fusion",
    lgd_col="lgd_obligor",
    year_col="year",
    seg_col="Seg",
    obligor_col="ID_CLIENT",
    poids_temporel=0.5,
    poids_segment=0.5,
)
print(df_calibration)

seuil_optimal = float(
    df_calibration
    .filter(pl.col("score_composite") == pl.col("score_composite").min())
    ["seuil_ovl"][0]
)
print(f"\nSeuil OVL retenu : {seuil_optimal}")
