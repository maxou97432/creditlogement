base_ml_full, outliers = detect_ano_ml_fct(input_df=cl_data, n_years = 12)

# DEBUG: Vérifier si VALEUR_FONCIERE existe
print(f"\nDEBUG - Colonnes dans base_ml_full:")
print(f"   VALEUR_FONCIERE existe: {'VALEUR_FONCIERE' in base_ml_full.columns}")
if 'VALEUR_FONCIERE' in base_ml_full.columns:
    print(f"   Valeurs non-NaN: {base_ml_full['VALEUR_FONCIERE'].notna().sum():,}")
else:
    print(f"   Colonnes disponibles: {sorted(base_ml_full.columns.tolist())}")
