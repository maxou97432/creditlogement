import pandas as pd
import numpy as np
import plotly.graph_objects as go
from statsmodels.tsa.arima.model import ARIMA
import plotly.io as pio
import warnings

# Configuration
warnings.filterwarnings("ignore")
pio.renderers.default = "notebook" 

# --- Initialisation ---
n_forecast = 2 
corrected_data = []
future_adjustments = []

grouped = merge.groupby(["VAL_MAILLE_GEO", "CD_TYPE_BIEN"])

print(" Démarrage de la modélisation ARIMA(4,1,3) (Graphique Historique + Print Prévision)...")

for name, group in grouped:
    zone, type_bien = name
    
    # 1. Préparation des données
    group = group.sort_values("Periode").set_index("Periode")
    group['Indice de prix (CL) - Corrigé ARIMA'] = np.nan
    
    # Série de l'écart
    ts_ecart = group['Dif'].dropna()
    
    if len(ts_ecart) > 15:
        try:
            # 2. Modélisation TES
            mod = ExponentialSmoothing(cv_train, trend='add', seasonal='add', seasonal_periods=4, damped_trend=True, initialization_method="estimated").fit()
            model_fit = mod.forecast(steps=test_horizon)
            
            # --- A. Correction Historique (Pour le Graphique) ---
            ecart_modelise = model_fit.fittedvalues.reindex(group.index)
            cl_corrected_history = group['Indice de prix (CL)'] - ecart_modelise
            group['Indice de prix (CL) - Corrigé ARIMA'] = cl_corrected_history
            
            # --- B. Calcul de la Prévision Future (Pour le Print uniquement) ---
            forecast_result = model_fit.get_forecast(steps=n_forecast)
            forecast_bias = forecast_result.predicted_mean
            conf_int = forecast_result.conf_int(alpha=0.05)
            
            # Stockage des valeurs pour le tableau
            future_adjustments.append({
                "Zone": zone,
                "Type": type_bien,
                "Ajustement T+1": round(forecast_bias.iloc[0], 2),
                "Ajustement T+2": round(forecast_bias.iloc[1], 2),
                "Marge Erreur T+1": round((conf_int.iloc[0, 1] - conf_int.iloc[0, 0])/2, 2)
            })

            # =================================================================
            # 3. Visualisation (HISTORIQUE UNIQUEMENT)
            # =================================================================
            
            fig = go.Figure()

            # Historique INSEE (Noir)
            fig.add_trace(go.Scatter(
                x=group.index, y=group['Indice de prix (INSEE) - Base 2020'],
                mode='lines', name='Indice de prix (INSEE)',
                line=dict(color='black', width=3)
            ))

            # Historique CL Brut (Rouge Pointillé)
            fig.add_trace(go.Scatter(
                x=group.index, y=group['Indice de prix (CL)'],
                mode='lines', name='Indice de prix (CL)',
                line=dict(color='red', width=1, dash='dot')
            ))

            # Historique CL Corrigé ARIMA (Bleu)
            fig.add_trace(go.Scatter(
                x=group.index, y=cl_corrected_history,
                mode='lines', name='Indice CL Corrigé (ARIMA)',
                line=dict(color='blue', width=2)
            ))

            # Pas de traces futures ici (supprimées comme demandé)

            fig.update_layout(
                title=f"<b>{zone} - {type_bien}</b> : Validation de la Correction ARIMA",
                yaxis_title="Indice (Base 2020 = 100)",
                template="plotly_white",
                height=500,
                legend=dict(orientation="h", yanchor="bottom", y=1.02, xanchor="right", x=1)
            )
            
            fig.show()
            
        except Exception as e:
            print(f"⚠️ Echec ARIMA pour {name} : {e}")
            
    corrected_data.append(group.reset_index())

# Reconstitution
merge_corrected = pd.concat(corrected_data, ignore_index=True)

# --- Affichage du Tableau de Bord (Prévisions) ---
if future_adjustments:
    df_previsions = pd.DataFrame(future_adjustments)
    print("\n" + "="*80)
    print(" VALEURS D'AJUSTEMENT PRÉVUES (ARIMA 4,1,3)")
    print("Ces valeurs sont à soustraire de l'indice CL futur pour obtenir l'estimation INSEE.")
    print("="*80)
    pd.set_option('display.max_rows', None)
    print(df_previsions)
else:
    print("\nAucune prévision générée.")
