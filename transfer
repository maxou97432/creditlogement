# Y-a-t-il un pattern dans les gros écarts ?
gros_ecarts3 = srs_X_capret3.with_columns(
    ((pl.col('MATURITE') + 1) / 2 / 12 - pl.col('NB_MATURITE_IRBA')).alias('Ecart')
)

# Répartition des écarts positifs (surestimation) vs négatifs (sous-estimation)
print("Ecart > 0.5 (surestimation) :", gros_ecarts3.filter(pl.col('Ecart') > 0.5).height)
print("Ecart < -0.5 (sous-estimation):", gros_ecarts3.filter(pl.col('Ecart') < -0.5).height)

# Est-ce que les gros écarts ont un MATURITE anormalement élevé ?
print("\nMATURITE des lignes avec écart > 1 an :")
print(gros_ecarts3.filter(pl.col('Ecart') > 1.0).select(['MATURITE', 'NB_MATURITE_IRBA', 'Ecart']).head(10))
